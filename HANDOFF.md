# 6th Seat Hold'em — Session Handoff

Running session log: what shipped, what's next, open items. Updated every session. Architectural decisions live in `SPEC.md`. This file is operational state only.

**Last updated:** 2026-05-14 (Session 25 — Greedy multi-denomination chip decomposition + offset-stacked single-chip rendering)

**Project completion estimate:** ~98% complete

## Session Prompt Standards — Required Boilerplate

Every session prompt written by Claude (in chat) for Claude Code (desktop) must include the start-of-session and end-of-session blocks below. These are non-negotiable workflow standards established across Sessions 1–15. Without them, Claude Code improvises and tells the user to run merge commands manually, which is a regression.

**START-OF-SESSION** (paste verbatim into every prompt):

- git checkout main
- git pull
- git log --oneline -5
- Confirm <expected HEAD commit> is HEAD on main. If not, STOP and report.
- If pbxproj is dirty (Xcode bookkeeping reorder), run `git restore SixthSeat.xcodeproj/project.pbxproj` before continuing
- Verify working tree is clean: `git status` should show "nothing to commit, working tree clean"

**WORKTREE PATH DISCIPLINE** (paste verbatim into every prompt that uses a worktree):

- All file writes in this session use absolute paths prefixed with the worktree path, never the main checkout path.
- Verify before the first write: `pwd` and `git rev-parse --show-toplevel` should both return the worktree path, NOT the main checkout path.
- If a file edit lands in the main checkout instead of the worktree (caught when tests still pass against the old behavior, or when `git status` in the worktree shows no changes after an edit), STOP. Recover by `mv`-ing the changes into the worktree path before any commit, and restore the main checkout to clean state with `git checkout` / `git restore`.
- This guard exists because Sessions 21 and 22 both surfaced the same write-to-main-checkout mistake mid-session. Both recovered cleanly, but the pattern is worth preventing.

**END-OF-SESSION** (paste verbatim into every prompt):

- Run all tests on both targets:
  - cd to engine package, run `swift test`, confirm engine count passing
  - Run `xcodebuild test` for the AppTests target, confirm app count passing
  - Combined total reported
- Build for iOS simulator, manually verify build succeeds (visual hardware verification will be done by David post-merge)
- Commit cleanly with descriptive message
- **Archive the session transcript BEFORE removing the worktree.** Claude Code's session storage is keyed by working-directory path. When the worktree directory is removed, Claude Code housekeeping empties the matching `~/.claude/projects/-Users-...-<worktree-name>/` directory, which deletes the `.jsonl` transcript of the entire session. Cost of skipping this step is losing the chat record of what was built and why — the code survives on `main`, but the conversational context (prompts, tool calls, intermediate decisions) is gone. To preserve:
  - Find the matching project directory: `ls ~/.claude/projects/ | grep <worktree-name>`
  - Copy any `.jsonl` files inside it to a safe location: `cp ~/.claude/projects/-Users-...-<worktree-name>/*.jsonl ~/claude-transcripts/session-<NN>.jsonl` (or any path outside `~/.claude/projects/`)
  - Then proceed with the merge + worktree removal below
- Manually merge to main with explicit commands (do not rely on the SessionEnd hook):
  - `git checkout main`
  - `git merge claude/<this-branch> --ff-only`
  - `git worktree remove <this worktree>`
  - `git branch -d claude/<this-branch>`
  - Confirm `git log` shows the new commit on main
  - Report the merge log
- Push to GitHub:
  - `git push`
  - Report any errors
- Report results, including:
  - Final test count (combined and per-target)
  - Confirmation of all `HANDOFF.md` / `SPEC.md` updates landed
  - Merge log
  - Push confirmation
  - Anything noteworthy from `/plan`
  - Any edge cases or latent-invariant findings

**WHY THIS MATTERS**

Claude Code handles its own merges. The user does not run merge commands manually. If a session prompt is missing this block, Claude Code may default to leaving the work on a worktree branch and asking the user to drive the merge — a workflow regression. Always include both blocks.

**Transcript preservation is non-obvious and irreversible.** The transcript-archive step above was added after Session 19a's chat record was lost to worktree removal. Claude Code's `~/.claude/projects/` storage is keyed by directory path, not by branch or session ID, so the moment the worktree path stops existing, the transcript at the matching project directory is housekept away. There is no undo. If a session involved meaningful design decisions worked out in chat (not just in code), archive the `.jsonl` before `git worktree remove` runs.

## Project History

| Session | Summary | Net lines |
|---|---|---|
| 14 | Main Menu screen + NavigationStack routing + persistent ChipStore | 224 |
| 14a | Bug fixes: bonus stacking on first launch, community cards face-down regression | 232 |
| 12 | Deal all 5 community cards face-down up front; flip on phase transitions instead of phase deal | 238 |
| 14c | Apply view-identity pattern to dealer hole cards (`.id()` + `Task.yield()`) | 242 |
| 12a | Unify Ante bet zone to tap-to-cycle (parity with Trips); remove +/- stepper | 250 |
| 12b | In-game bust flow: first-bust gift modal awards 2,500 chips, second-bust modal routes to Chip Shop; Chip Shop stub upgraded with back-to-menu | 267 |
| 12c | Doc cleanup: consolidate architectural decisions to `SPEC.md`, record latent-invariant audit pattern as a Workflow Lesson | 267 |
| 12d | Affordability gates and bust threshold correctness: DEAL gated on 6× Ante (worst-case main bet), Trips force-cleared when unaffordable, bust threshold raised to `minimumPlayableBalance` (2× minimum chip = $10) | 282 |
| 15a | Submission-ready surfaces: first-launch Apple 4.3 disclosure modal, real Settings screen (audio/haptics/legal/about), real How to Play screen with engine-sourced paytables, Chip Shop no-cash-value reinforcement | 311 |
| 15b | Table selection UI + table-aware cycle ranges via `TableConfig` (engine), per-bet payout breakdown on hand-result UI, centralized cycle constants | 361 |
| 15c | Revert 15b's per-bet payout breakdown after hardware-test feedback; hand-result UI now shows a single colored net-result number (`+$N` green / `-$N` red / `Push` neutral) | 351 |
| 16 | Real Chip Shop with StoreKit 2 IAP. Five consumable tiers, per-install first-purchase doubler, restore button, persistent transaction listener. Engine-fronted IAP service + processor + telemetry (all SwiftUI-free); local Configuration.storekit for simulator testing. | 411 |
| 17 | Asset pipeline scaffolding. AssetService protocol + BundleAssetService + InMemoryAssetService, 83-slot placeholder asset catalog (52 cards + 1 back + 5 chips + 25 stack variants), engine-side StackHeight enum with bestFit rounder, CardView/ChipView/ChipStackView consume assets via SwiftUI environment. Drop-in ready for the Fiverr Phase-1 PNGs. | 433 |
| 18a | Phone-test pass against Sessions 11/12/14/14a/15a/15b/15c/16/17 backlog on iPhone 16 Pro Max running iOS 26.4.2. No new code shipped. Cleared 18 of the 21 effective items (✅ 18, ⚠️ 1, ❌ 5, ⏳ 3 not-organically-observed). Surfaced three new bugs for Session 18b+: (1) `TableConfig.anteRangeDescription` formats `$50 – $1000` without comma — currency-convention drift from 2026-05-04; (2) `docs/privacy.html` and `docs/terms.html` were exported from TextEdit and contain escaped HTML/CSS template markup as visible text instead of legal copy — App Store submission blocker; (3) **stranded-balance regression** — at `$30` the player is above `GameConstants.minimumPlayableBalance` (`$10`) so no bust trigger fires, but below the cheapest table's `minimumEntryBalance` (`$60` at `.table10`) so no table is enterable, and the second-chance bonus path never runs. The bug was introduced when Session 15b added higher-stake tables without making the bust threshold table-aware; Session 12d's "no stranded balance" guarantee no longer holds. | 433 (unchanged) |
| 18b | Bug-fix pass driven by 18a's findings. (1) Stranded-balance fix — `GameConstants.minimumPlayableBalance` now delegates to a new `TableConfig.cheapestEntryBalance` static (currently `$60`) so the bust trigger and menu-boundary fallback automatically track stake-level changes. All four sites (`BonusLogic.applySecondChanceBonusIfEligible`, `GameTableViewModel.maybeShowBustFlash`, `MainMenuLogic.playEnabled`, `MainMenuLogic.handlePlayTap`) keep reading the same constant — single source of truth preserved. New regression test `secondChanceBonusFiresWhenStrandedBetweenStakeMinimums` asserts the bonus fires from `$30` and locks in the table-aware invariant against future stake changes. Existing `BustFlowTests.bustDoesNotFireAtThreshold` recalibrated for the new `$60` boundary; `GameConstantsTests.minimumPlayableBalanceTracksCheapestTableEntry` updated to assert the new derivation. (2) Currency drift fix — `TableConfig.anteRangeDescription` routes both range ends through `ChipShopLogic.formatChipAmount` so the `$50` card now renders `$50 – $1,000` (was `$50 – $1000`); `anteRangeDescriptionIsCorrect` test updated to match. Per the SPEC's latent-invariant Workflow Lesson, both bugs are documented in the Resolved Scope Drift Log so the lesson outlives this session. Test count: 434 (185 engine + 249 app, +1 from Session 17 — the new regression test). | 434 |
| 18c | DEBUG-only deterministic deal-forcing affordance to unblock the three ⏳ items from Session 18a (dealer-doesn't-qualify / community-board verification / push outcome). Three layers, all wrapped in `#if DEBUG` so they don't compile into Release: (a) `Deck.init(forcedDealOrder:)` builds a deck whose `deal()` returns the supplied cards in order; (b) `GameState.setForcedDeck(_:)` swaps the engine's deck so the next `deal()` pulls from the forced sequence — single-shot (`collectAndReset()` reshuffles to a normal random 52-card deck); (c) new `DebugScenario` enum + `DebugDealForcer.pendingScenario` static (`nonisolated(unsafe)`) expose three preset 9-card sequences hand-verified against the real `HandEvaluator`. The buffer is consumed and cleared by `GameTableViewModel.deal()` before the engine dispatch. Hidden access: long-press the Main Menu title (1.5s, `LongPressGesture(minimumDuration: 1.5)`) reveals `DebugMenuView` as a `.sheet` — no on-screen affordance hints at its existence. Five new engine tests under a `DebugDealForcer (DEBUG-only)` suite: `Deck(forcedDealOrder:)` order, `GameState.setForcedDeck` deal sequence, and one outcome assertion per scenario. Verified empty in Release: `strings` finds zero references to `DebugDealForcer / DebugScenario / DebugMenuView / forcedDealOrder / setForcedDeck` in the Release `SixthSeatApp` binary; the same symbols are all present in the Debug build's `SixthSeatApp.debug.dylib`. Test count: 439 (190 engine + 249 app, +5 from Session 18b). | 439 |
| 19a | Audio system foundation. New App-layer `AudioService` protocol + `SoundEffect` enum (10 cases — one per designer-shipped CAF in `App/Resources/Audio/`). `AVAudioService` (production) configures `AVAudioSession` for `.ambient` + `.mixWithOthers` on init so SFX coexist with the player's background music without taking an exclusive lock, caches one `AVAudioPlayer` per `SoundEffect` for repeat-play latency, and reads `PersistenceKeys.settingsSFXEnabled` at the call site so the Settings toggle takes effect immediately. `InMemoryAudioService` (test double) records every `play(_:)` request without performing playback. SwiftUI `\.audio` environment wired in `SixthSeatApp` so previews and views can override; `GameTableViewModel.init` accepts an `audio:` param defaulting to a fresh `AVAudioService`. Trigger map wired into the view model: player hole-card reveal fires `.cardDeal`, community reveal fires `.cardPlace`, dealer showdown fires `.cardFlip`, Ante/Trips cycle fires `.chipPlace`, Play bets fire `.chipStackHandle`, fold fires `.fold`, flush-or-better wins fire `.winBig` + `.chipPayoff`, sub-flush wins fire `.winSmall` + `.chipPayoff`, losses fire `.loss`, pushes stay silent. The 10 CAF files (`card_deal`, `card_place`, `card_flip`, `chip-_place`, `chips-handle-6`, `chip_payoff`, `fold`, `win_small`, `win_big`, `loss`) ship as a folder reference at `App/Resources/Audio/` so adding a new SFX is now drop-in. **Music toggle removed** from `SettingsView` along with `PersistenceKeys.settingsAmbientEnabled` and the matching assertion in `SettingsViewTests` — no music asset was sourced for V1, so the toggle had nothing to gate; Music returns as a V2 candidate. SPEC's Resolved Scope Drift Log entry dated 2026-05-11 records the decision. Test count: 448 (190 engine + 258 app, +9 from Session 18c — 3 `AudioService` tests + 6 SFX-wiring assertions in `GameTableViewModelAudioTests`). On-device verification of SFX, the Sound Effects toggle, silent-switch behavior, mix-with-others against Spotify, latency/glitch checks, and the two-row Settings layout against the designer's panel art is the next phone-test pass. | 448 |
| 19b | Two-part session driven by the Session 19a phone test + the long-deferred Session 18 TelemetryDeck scope. **(1) Dealer-showdown SFX fix.** `animateDealerHoleCards` now fires `.cardFlip` exactly once, synchronously, before its first `await Task.yield()` — was firing per dealer card via `revealWithHaptic(_:sfx:)` and the cached `AVAudioPlayer.currentTime = 0; play()` 200ms later was restarting the first instance mid-playback (the 0.92s `card_flip.caf` is longer than the inter-card gap). Net effect on iPhone 16 Pro Max: silent showdown. Single-fire at the top also lands the SFX before any `await`, so a same-frame `skipToSettled` still produces audio. As defense-in-depth, `finalizeSettledState` fires `.cardFlip` once when dealer cards are being newly-revealed (catches the `bypassAnimation` test path and the skip-mid-dealer-reveal user path) — checks `revealedCards` first to avoid double-firing on the normal path. Existing `Showdown fires cardFlip × 2` test updated to assert `× 1` (single-fire contract); new `Dealer cardFlip SFX fires via the bulk-reveal fallback when the dealer-reveal animation is bypassed` test pins the finalize-time fallback; new `Dealer cardFlip SFX does not double-fire when the animation runs to completion` test pins the no-double-fire invariant. **(2) TelemetryDeck wiring.** Pulled in `TelemetryDeck/SwiftSDK` via the local `SixthSeat` Swift package (resolved at 2.12.0). New `TelemetryDeckTelemetryService` (engine package) conforms to the existing `TelemetryService` protocol — Session 16's protocol was set up specifically so this swap is drop-in, and no existing method signature changed. Added one new protocol method `handResolved(tableID:anteAmount:tripsAmount:resultTone:tripsOutcome:)` plus `HandResultTone` (`win/loss/push`) and `TripsTelemetryOutcome` (`paid/lost/notPlaced`) enums in the engine. `SixthSeatApp.init` calls `TelemetryDeck.initialize(config:)` before `iap.startTransactionListener()` so background `Transaction.updates` deliveries always dispatch to a live SDK; DEBUG builds use `LoggingTelemetryService` instead so the dev loop doesn't burn quota with noise. App ID is the hardcoded placeholder `"TELEMETRYDECK_APP_ID_PLACEHOLDER"` with a `// TODO: David` — verified to ship in the Release `SixthSeatApp` binary via `strings`. Signal-name convention `namespace.event`: `iap.purchase.initiated/succeeded/failed`, `iap.restore.initiated/completed`, `game.hand.resolved`. Purchase-succeeded payload: product_id, base_chip_amount (derived from `ChipBundleCatalog`), doublered_chip_amount, doubler_fired, is_first_purchase. Hand-resolved payload: table_id, ante_amount, trips_amount, result_tone, trips_outcome. Balance amounts deliberately NOT logged (privacy — derivable from purchase + game history server-side). Hand telemetry fires from `syncFromGame` on the `previousPhase != .handComplete && game.phase == .handComplete` transition (same beat as `lastAnteBet` / `lastTripsBet` snapshotting). Tests: 8 engine tests in a new `TelemetryDeckTelemetryService payload shape` suite assert the signal-name + parameter-dict contract directly via testable static helpers; 3 app tests in a new `GameTableViewModelTelemetryTests` suite assert call-site shape via `RecordingTelemetryService`. SPEC's Architectural Decisions section gains two new entries (dealer SFX single-fire, TelemetryDeck integration); Drift Log gains the 2026-05-11 chip-place-SFX-at-bet-time entry. Test count: 461 (198 engine + 263 app, +13 from Session 19a — 8 engine + 5 app). | 461 |

| 20 | Fiverr Phase 1 asset integration. 51 card faces sliced from the designer's `Vector illustrations/Card Decks/poker cards@4x 1.png` sheet via a new `tools/slice_card_sheet.py` (kept in-repo for any future re-slice); the 7-of-Diamonds slot was a designer-delivery bug (sheet contained a duplicate Ace of Diamonds where 7D should have been) — David supplied a standalone `Asset 2.png` (7D), renamed to `card_diamonds_7_standalone.png`, copied into the 7D imageset. Card back sourced from `Card back.png`. Chips: 5 singles (`Vector illustrations/Coins/$X/x1.png`) + 25 stack variants (`x{1,3,5,10,20}.png` per `$5/$25/$100/$500/$1000` denomination) — `x1.png` files copied twice per denomination, once as the single `chip_X.png` and once as the `stack_X_h1.png` 1-chip variant. All 83 asset slots that Session 17 scaffolded are now populated with real designer art (52 cards + 1 back + 5 chips + 25 stacks), matching the catalog exactly — Session 17's drop-in-ready design worked as expected, zero view code changed. On-device Debug-build verification on iPhone 16 Pro Max confirmed cards render correctly; engine math, hand resolution, and payout headlines all functional with the new art. Chips not yet surfacing on the game table — expected, because bet-zone chip-stack visuals require view code that wasn't part of Session 17 scaffolding (still deferred — see Open Items). Obsolete pre-delivery folders `Design Assets/` and `Final assets - V2/` deleted; `.gitignore` updated to exclude `SixthSeat.xcodeproj/project.xcworkspace/xcshareddata/` and `.claude/settings.local.json`. Bonus Fiverr UI chrome (app icons, button states, popup chrome, panels, backgrounds, table felt) remains staged in `~/Desktop/fiverr-phase1/` for Phase 2 integration. Test count unchanged at 461 (198 engine + 263 app) — assets don't touch engine or app logic. Two commits as required (tooling/gitignore + asset integration & documentation). | 461 (unchanged) |

| 22 | UI fix-pack driven by the Session 21 phone test on iPhone 16 Pro Max. Three surgical fixes, no new architecture. **Part 1 — broken $5 chip art recropped.** The `stack_5_h1` and `chip_5` PNGs from Fiverr arrived at `3661×1909` with the chip drawn in the bottom-right corner of an otherwise-transparent canvas; every other denomination's h1 art and every `chip_X` came tight-cropped at `~800×740`. The bet circle's `40×40 .aspectRatio(.fit)` frame scaled the broken $5 art's chip down to an ~8px speck (visible on hardware as a tiny dot when Ante was set to $10 → `(5, 2)` → `stack_5_h1`). Fix: cropped both `chip_5.png` and `stack_5_h1.png` in place to the chip's visible bounding box (`801×746`), preserving the artwork pixel-for-pixel — no Fiverr redelivery needed. Guard test in `AssetPipelineTests` (`ChipArtDimensionsTests` suite, 2 new tests) loads each `chip_<denom>` and `stack_<denom>_h1` imageset via `UIImage(named:)` and asserts the aspect ratio falls in `[0.7, 1.3]`, locking out any future oversized-canvas redelivery. **Part 2 — `ChipDecomposition.bestFit` returns accurate multi-chip stacks.** Session 21's algorithm picked the largest denomination ≤ `amount` and divided regardless of clean fit, so $50 rendered as `(25, 1)` — visually identical to a $25 bet. New algorithm picks the largest denomination D where `amount % D == 0`, yielding `$50 → (25, 2)` / `$75 → (25, 3)` / `$1500 → (500, 3)` / `$35 → (5, 7)` (fall back to $5 chips when no larger denom divides cleanly). Single-character change (`&& amount % denom == 0`) in `SixthSeat/Sources/SixthSeat/ChipDecomposition.swift`; docstring rewritten to reflect "exact representation, not approximation." Existing `offGridApproximations` and `picksLargestAvailableDenomination` tests renamed and updated with the new expectations ($35→(5,7), $250→(25,10), $1500→(500,3)); new `multiChipDecompositionsAreVisuallyDistinct` test pins five representative amounts ($50/$75/$125/$300/$600). The `BetZoneViewChipStackTests` Session 21 assertions still pass unchanged because the affected amounts ($10, $100) already produced the same result under both algorithms. **Part 3 — New Hand clears staged Ante to $0.** Session 21's `newHand()` only cleared `stagedTrips`; `stagedAnte` carried over from the previous hand, so the only way to "start a fresh bet" was to cycle the Ante zone all the way around back to $0. Fix: `GameTableViewModel.newHand()` now also sets `stagedAnte = 0`. The Rebet path is unchanged — it still restores `lastAnteBet` and (if affordable) `lastTripsBet` from the prior hand. New `newHandClearsStagedAnte` test in `GameTableViewModelTests`. Latent-invariant audit surfaced one existing test (`AnimationTests.dealIdStableOnNewHand`) that implicitly relied on `stagedAnte` carrying over — updated to re-stage Ante before the second deal. **Tests.** Engine: 207 (was 206, +1 — `multiChipDecompositionsAreVisuallyDistinct`). App: 269 (was 266, +3 — 2 chip-art dimension guards + 1 `newHand` Ante-clear assertion). Combined: 476 (was 472). Full `xcodebuild test` pass on iPhone 17 Pro Max simulator. **Asset note.** Recrop runs entirely on the existing $5 chip PNGs (no Fiverr redelivery needed); the operation is non-destructive in the sense that the chip art itself is pixel-identical — only the transparent padding around it was removed. Two commits as required (engine + tests; views + asset crops + docs). | 476 |
| 21 | UI polish driven by the Session 20 phone-test gaps — first real view-code session since the Session 17 asset-pipeline scaffolding. **Part 1 — bet-zone chip stacks.** New engine helper `ChipDecomposition.bestFit(for amount: Int) -> ChipDecomposition?` in `SixthSeat/Sources/SixthSeat/ChipDecomposition.swift` returns `(denomination, count)` — the largest available chip denomination ≤ amount and `amount / denomination` (integer division). Returns `nil` for `amount ≤ 0` and for amounts below the smallest chip ($5). Off-grid amounts accept a visual approximation (e.g. $35 → ($25, 1)) because the precise dollar value is always rendered on the adjacent label — same "START SIMPLE" pattern called out in the Session 21 prompt. `ChipDecomposition` follows the same pure-engine pattern as `StackHeight.bestFit` (Session 17) and is asserted directly by 8 engine tests across exact denominations, multiples, off-grid amounts, large amounts, zero, negative, and sub-$5. `BetZoneView` rewritten to render the label above the bet circle, a `ChipStackView` inside the circle (when amount > 0, using the chip decomposition), and the dollar amount label below — the chip stack scales/slides/fades with the rest of the zone during chip-resolution animation so motion stays coherent. Empty bet zone renders the circle with "—" inside (no chip stack). **Part 2 — card sizing 1.4×.** `GameTableView` now scales hole cards (player + dealer) from 60×84 → 84×118 and community cards from 52×72 → 73×101, uniformly 1.4× from the pre-Session-21 baseline. Same scale across all three slots (no special-case bigger hole cards); aspect ratio preserved. The frame constants live as private `holeCardWidth/Height` + `communityCardWidth/Height` on `GameTableView` and are passed to each `CardView` site explicitly. Project Convention #4 per-deal `.id("...")` modifiers and `Task.yield()` animation entry points preserved at every CardView site. **SFX phantom-feel resolved.** The 2026-05-11 SPEC Drift Log entry (chip-place SFX firing at bet time without a chip-stack visual on the felt) is now closed — bet-time SFX, balance decrement, and chip-stack appearance fire on the same beat. Status moved from `⏳ resolves with Fiverr asset drop` to `✅ in build [Session 21]`. **Tests + audit.** 8 new engine tests in `ChipDecompositionTests`; 3 new app tests in `BetZoneViewChipStackTests` (non-zero bet → chip stack request via `InMemoryAssetService`; zero bet → no request; largest-denomination selection). Latent-invariant audit (per SPEC's Workflow Lesson) cleared — no existing test asserts BetZoneView's internal structure or card frame dimensions, so no breakage. **Simulator verification.** Debug build for iOS simulator succeeded (`xcodebuild build` → `BUILD SUCCEEDED`); full `xcodebuild test` pass on iPhone 17 Pro Max simulator (266 app tests passing). First-launch disclosure modal renders correctly on simulator; programmatic tap-through to game table wasn't available in this session (no UI test target, no `simctl tap` primitive, `osascript` System Events click blocked) so the chip-stack-on-felt visual is for David's on-device pass to verify. Test count: 472 (206 engine + 266 app, +11 from Session 20 — 8 engine + 3 app). Two commits as required (engine helper + tests; view code + asset wiring + docs). | 472 |

| 23 | Card-sheet reslice with programmatically uniform crop boundaries — direct response to Session 22 phone test on iPhone 16 Pro Max, which surfaced that the Fiverr Phase 1 sliced cards had visible margin inconsistency (one card's corner marker sitting slightly closer to the edge than another's). Investigation showed the cards themselves were within 1px of each other in width/height, but `tools/slice_card_sheet.py`'s hardcoded `COL_BOUNDS` / `ROW_BOUNDS` had inter-column gaps varying between 3–6 pixels; at on-device render scale, small crop-boundary variance translated to visible margin differences. **Fix.** Slicer now derives uniform boundaries from the source sheet at runtime via alpha-channel detection + least-squares fit. Per-column and per-row "any-opaque" flags identify card runs along each axis; the median run width gives `card_width` / `card_height`, and a linear least-squares fit over the cleanly-detected card centers (`center[i] = first_center + i × pitch`) gives uniform spacing. Fused runs (two adjacent cards with no alpha-transparent gap between them — happens on rows 6–7 of this sheet) are detected by inflated run width and their grid indices skipped during the fit; the projected pitch covers them. Derived values: `card_width = 125 px`, `card_height = 174 px`, `column_gap = +1.589 px`, `row_gap = +2.050 px`. Residual error vs the pre-Session-23 hardcoded values is at most ±1.5 px on column centers and ±0.5 px on row centers — well within the plan's 2 px tolerance. New `--debug` flag prints the derived values + per-bound residuals for verification. The `LAYOUT` dict and the known-bug skip for the duplicate Ace of Diamonds at sheet position (2, 5) are unchanged; the standalone `card_diamonds_7.imageset/card_diamonds_7.png` (David-sourced replacement for the missing 7D) is preserved by the slicer's pre-existing "no sheet position maps to ('diamonds', '7')" handling, verified by SHA before/after the reslice. **Output.** 51 sheet-derived cards rewritten with identical 125×174 pixel dimensions; card back re-saved byte-identical (single-source file); standalone 7D untouched. **Tests.** Engine: 207 (unchanged from Session 22). App: 269 (unchanged from Session 22). Combined: 476 (unchanged). Per-card dimension uniformity is guaranteed at the script level (the slicer cannot produce non-uniform output by construction), so a `CardArtDimensionsTests` suite was considered and skipped — would require loading 52 imagesets via `UIImage(named:)` for what the script already guarantees structurally. Full `xcodebuild test` pass on iPhone 17 Pro Max simulator; Debug build for iOS simulator succeeded. **No SPEC change** — reslicing is a script-quality + re-execution of Session 17 scaffolding, not an architectural decision. One commit (slicer change tightly coupled to the resliced PNGs — separating them would create an in-between state where the script doesn't match the asset catalog). | 476 (unchanged) |

| 24 | Per-card bbox detection + uniform-canvas padding in `tools/slice_card_sheet.py`. Direct response to the Session 23 follow-up surfaced in chat: Photoshop verification on the source sheet showed the designer's cards sit at slightly different vertical positions within their grid cells, so Session 23's uniform-pitch crops landed each card's art at a different vertical offset inside an otherwise pixel-identical output PNG — some cards' bottom margins read clipped on-device, others rendered with extra transparency. **Fix.** Slicer keeps the Session 23 alpha-derived uniform grid as a *search region* and now runs PIL's `Image.split()[-1].getbbox()` inside each grid cell to detect the card art's tight opaque bounding box per-card. The detected bbox center captures the per-card shift; tight crops are then padded to a single uniform output canvas (`max(detected_bbox_w) × max(detected_bbox_h)` across all 51 sheet cards) with the art centered and transparent margins added on all four sides. Search margin is held at `0` because the source sheet's inter-card gap is ~1–2 px (row 6/7 even fuse into a single alpha run); any margin > the gap would let neighboring card art bleed into the search region (verified empirically — margin=20 produced 165×214 bboxes contaminated by neighbors). With margin=0 the bbox detection finds tight extents `123–125` wide × `171–174` tall (avg `124.06 × 172.80`); target canvas resolved to `125 × 174` — identical pixel dimensions to Session 23 output, but each card's art is now centered to ≤1 px tolerance rather than carrying its source-sheet offset. **Sanity gate.** Any detected bbox whose width or height differs from the Session-23 uniform `card_width`/`card_height` by > 50% aborts the run before any PNG write (`BBOX_SANITY_FRACTION = 0.5`). All 51 bboxes passed; all 51 had centers within 5 px of the uniform nominal. **Output.** 51 sheet-derived cards rewritten with identical 125×174 pixel dimensions; card back re-saved byte-identical; standalone 7D untouched (`shasum` confirmed `40766b47…` before and after). The `LAYOUT` dict, the duplicate-Ace-of-Diamonds skip at (2,5), and the missing-7D handling are unchanged. **Tests.** Engine: 207 (unchanged). App: 269 (unchanged). Combined: 476 (unchanged). Per-card dimension uniformity is still guaranteed by construction; the new contribution is per-card art centering on a uniform canvas, which is also a script-level guarantee. **Architectural decision** added to `SPEC.md` (one short entry — card art is padded to a uniform canvas at slice time, not at render time, so view code can rely on every card asset being dimensionally identical and visually centered). One commit (slicer change + resliced PNGs + docs together — same reasoning as Session 23, separating them creates a script/catalog mismatch state). | 476 (unchanged) |
| 25 | Bet-zone chip rendering rebuilt around the casino-canonical visual: mixed-denomination chips physically stacked, largest on the bottom, with the rim of each chip showing above the chip below. Driven by Session 22/24 phone-test feedback that the pre-rendered single-denomination stack art (e.g. `stack_5_h3.imageset` for $15 = three $5 chips) doesn't match how a real felt looks for amounts like $125 (one $100 + one $25). **Engine.** `ChipDecomposition` redesigned from `bestFit(for amount: Int) -> ChipDecomposition?` (single denomination/count pair, Session 22 clean-divisor contract) to `decompose(amount: Int) -> [ChipChunk]` (greedy largest-first decomposition returning chunks ordered largest to smallest). Examples: `$30 → [($25,1), ($5,1)]` / `$125 → [($100,1), ($25,1)]` / `$1235 → [($1000,1), ($100,2), ($25,1), ($5,2)]`. Empty array for `amount ≤ 0`. Old `bestFit(for:)` method deleted entirely — every call site moves to `decompose(amount:)`. **View.** `ChipStackView` rewritten: takes `[ChipChunk]`, walks chunks largest-first, renders each chunk as N copies of `chip_<denomination>.imageset` (single-chip art, not pre-rendered stack art) in a `ZStack(alignment: .bottom)` with each subsequent chip offset upward by `perChipOffset`. Session 25 defaults: `chipDiameter = 40`, `perChipOffset = 8` (~20% of chip diameter — rim-of-chip-above-rim visual). The bottom chip is the largest denomination, matching real felt convention. `BetZoneView` switched from `let chip = ChipDecomposition.bestFit(for: amount)` to `let chunks = ChipDecomposition.decompose(amount: amount)`. **Stack imagesets unused.** The 20 `stack_<denom>_h<height>` imagesets (`h3 / h5 / h10 / h20` per denomination × 5 denominations) in `App/Assets.xcassets/ChipStacks/` are now unreferenced by production code; the `h1` variants are still loaded by `ChipArtDimensionsTests`. Marked for cleanup in a focused housekeeping session (see Follow-ups). `StackHeight` enum and `AssetService.chipStackImage(denomination:height:)` remain in the engine/app code; their only consumers now are the `InMemoryAssetService` self-tests and `AssetNames.stack` mapping tests. **Tests.** Engine: 207 (unchanged — 9 old `ChipDecompositionTests` removed, 9 new ones added covering the chunk-ordering, positive-count, sum-equals-input, and example-amount invariants). App: 272 (was 269, +3 — 2 stale `ChipStackView`/`StackHeight.bestFit`-routing tests removed and 3 new ones added in `ChipViewRenderingTests` for the offset-stack rendering path; 3 old `BetZoneViewChipStackTests` rewritten and 2 added covering multi-denomination + worst-case-shape `$1235`). New `expectChipRequestPattern` test helper tolerates SwiftUI's multi-pass `ForEach` evaluation under `ImageRenderer` by asserting the first cycle matches and the log length is a positive multiple. Combined: 479. **Latent-invariant audit** cleared — only one production call site (`BetZoneView`), the SwiftUI `BetZoneView` external surface (label/amount/animation/onTap) is unchanged so the four `GameTableView` instantiations need no edits, and the `RecordingTelemetryService` paths don't touch chip rendering. The 2026-05-11 Drift Log entry on chip-place SFX phantom-feel stays `✅ in build [Session 21]` — SFX still fires at bet placement, chip visual still appears at the same beat. SPEC's Architectural Decisions entry on `ChipDecomposition` rewritten to reflect the greedy multi-denomination contract; a new entry covers the offset-stack rendering approach. Two commits as required (engine + tests; view code + asset references + docs). | 479 |

(Earlier sessions 1–11 are reconstructable from `git log --oneline` on `main`.)

## Current File Inventory (post-Session 17)

App/Views additions and annotations:

* `CardView.swift` (Session 17 — slot-based renderer, consumes `AssetService` via `\.assets` SwiftUI environment. Signature switched from `faceDown: Bool` to `faceUp: Bool` to match the Session 17 prompt; existing call sites in `GameTableView` negate the view-model's `isXCardFaceDown(index:)` accessors at the boundary. Empty-slot rendering (when `card == nil`) is unchanged dashed outline; face/back rendering now resizes the named asset image (`card_<suit>_<rank>.png` / `card_back.png`) inside a rounded clip. The Y-axis flip animation and per-deal `.id("...")` view-identity convention are preserved.)
* `ChipStackView.swift` (Session 17 — signature changed from `amount: Int` to `(denomination: Int, count: Int)`. Reads `\.assets` environment, picks variant via `StackHeight.bestFit(for: count)`, renders `stack_<denomination>_h<height>.png`.)
* `ChipView.swift` (Session 17 — new view. `denomination: Int` parameter, reads `\.assets`, renders `chip_<denomination>.png`.)
* `MainMenuView.swift` (encloses `MenuDestination` enum + `MainMenuLogic` helpers; PLAY now routes to `.tableSelect` instead of straight to the game)
* `TableSelectView.swift` (Session 15b — three-card picker reading `TableConfig.all`. `TableSelectLogic.canEnter` / `allTablesUnaffordable` / `resolveLastPlayed` are pure helpers exposed for tests. Persists `PersistenceKeys.selectedTableID` via `@AppStorage` on tap, before the navigation push)
* `GameTableView.swift` (Session 15c — `.handComplete` headline restored to a single signed `+$N` / `-$N` / `Push` Text colored green / red / white off `HandResultHeadline.tone(for:)`. The `HandResultHeadline` enum is a pure helper at the bottom of the file, exposed for unit tests so the tone/text mapping can be asserted without rendering SwiftUI)
* `SettingsView.swift` (Session 15a — Form-based screen with three sections: Audio & Haptics toggles, Legal & Disclosures with hosted GitHub Pages links, About with bundle version. `SettingsLinks` exposes the privacy/terms URLs and version-string formatter for tests)
* `ChipShopView.swift` (Session 16 — replaced the stub with the real shop. Header shows current chip balance, doubler banner renders while `hasMadeFirstPurchase == false`, five tier cards iterate `viewModel.bundles` rendering doubled / strikethrough amounts via `ChipShopLogic`, per-card buy button shows StoreKit-localized price (or a spinner during purchase, or an inline error on failure), Restore Purchases button at bottom, no-cash-value reinforcement line and Back to Menu retained. View is dumb — every behavior lives in `ChipShopViewModel`. Stable identifiers: `ChipShop.Balance`, `ChipShop.DoublerBanner`, `ChipShop.Buy.<id>`, `ChipShop.Amount.<id>`, `ChipShop.Strikethrough.<id>`, `ChipShop.Badge.<id>`, `ChipShop.Error.<id>`, `ChipShop.Restore`, `ChipShop.RestoreMessage`, `ChipShop.NoCashValue`, `ChipShop.BackToMenu`.)
* `HowToPlayView.swift` (Session 15a — single scrollable rules reference with engine-sourced paytables. `HowToPlayCopy.blindRows` / `tripsRows` derive from `UTHRules.blindPaytable` / `tripsPaytable` so spec/UI cannot drift)
* `BustFlashView.swift` (Session 12b — in-game flash modal; first-bust gift, second-bust Chip Shop routing)
* `DisclosureModalView.swift` (Session 15a — Apple 4.3 first-launch entertainment-only disclosure. `DisclosureCopy` shares the title/body strings with the Settings legal section)

App/ViewModels additions:

* `GameTableViewModel.swift` (Session 15b — accepts `TableConfig` at init, default `.table10`. `anteCycle` / `tripsCycle` are computed off `tableConfig`; `stagedAnte` initializes to `tableConfig.minimumAnte`. The Trips zone affordability gate reads `tableConfig.minimumTripsStep` instead of a global `minimumChipValue`. First-bust reset uses `tableConfig.minimumAnte` instead of a hardcoded `5`)
* `GatedHapticsService.swift` (Session 15a — wrapper over any `HapticsService` that reads `PersistenceKeys.settingsHapticsEnabled` at the call site. Default-on. Production `GameTableViewModel.init` wraps `SystemHapticsService` in this gate)
* `ChipShopViewModel.swift` (Session 16 — `@Observable @MainActor` view model. Owns the displayed bundles array (initialized to `ChipBundleCatalog.all` so the screen renders immediately, refreshed in-place by `loadProductsIfNeeded()`), per-bundle loading + error state, balance + `hasMadeFirstPurchase` mirror, restore state. `purchase`/`restore` route into the injected `IAPService`. `displayAmount` / `strikethroughAmount` / `doublerActive` derive from `ChipShopLogic` so the doubler math is testable without rendering SwiftUI. Concurrent purchase taps are dropped at the boundary (`loadingBundleID` guard); an additional engine-level dedupe via `processedTransactionIDs` is the load-bearing safety net.)
* `AssetService.swift` (Session 17 — App layer service. Defines the `AssetService` protocol (`Sendable`), `AssetNames` pure name-mapping helper, `BundleAssetService` (production, returns `Image(<name>)`), and `InMemoryAssetService` (`@unchecked Sendable` test double that records every request and returns SF Symbol images). Also defines the SwiftUI `\.assets` environment value with `BundleAssetService()` as the default. Lives alongside `HapticsService` / `SystemHapticsService` / `GatedHapticsService` in `App/ViewModels/` to match the existing service-injection pattern.)
* `AudioService.swift` (Session 19a — App layer service. Defines the `AudioService` protocol (`Sendable`), `SoundEffect` enum (10 cases, raw value = bundled CAF filename without extension; `bundleSubdirectory = "Audio"` + `fileExtension = "caf"` constants), `AVAudioService` (production; `@unchecked Sendable`; lazily caches one `AVAudioPlayer` per `SoundEffect` keyed by enum case; configures `AVAudioSession` for `.ambient` + `.mixWithOthers` on init under `#if os(iOS) || os(tvOS) || os(watchOS)`; reads `PersistenceKeys.settingsSFXEnabled` at the call site so the Settings toggle takes effect immediately), and `InMemoryAudioService` (test double; records each `play(_:)` request to `playLog` and each `setSFXEnabled(_:)` to `enabledLog`; `reset()` clears both and re-enables). Also defines the SwiftUI `\.audio` environment value with `AVAudioService()` as the default. Lives alongside `AssetService.swift` and the haptics trio in `App/ViewModels/`.)

Engine package additions:

* `TableConfiguration.swift` (Session 15b — `TableConfig` struct: `Equatable`, `Hashable`, `Codable`, `Identifiable`, `Sendable`. Three V1 tables (`.table10` / `.table25` / `.table50`) and helpers (`minimumEntryBalance`, `minimumTripsStep`, `anteRangeDescription`, `table(forID:)`). Single source of truth for cycle data — the app reads cycles from a `TableConfig` instance, never inline literals)
* `UserDefaultsChipStore` (real production implementation, applies starter bonus eagerly on init)
* `PersistenceKeys.hasSeenDisclosure` (Session 15a — gates first-launch disclosure modal)
* `PersistenceKeys.settingsSFXEnabled` / `settingsAmbientEnabled` / `settingsHapticsEnabled` (Session 15a — `@AppStorage`-backed user preferences. SFX/ambient store-only until Session 17 audio integration; haptics gates immediately via `GatedHapticsService`)
* `PersistenceKeys.selectedTableID` (Session 15b — last-played `TableConfig.id`. Default-handling lives in `TableConfig.table(forID:)`; an unset or unknown id resolves to `TableConfig.defaultTable`)
* `ChipBundle.swift` + `BundleBadge` (Session 16 — `Equatable, Hashable, Codable, Identifiable, Sendable` struct. Engine ships placeholder `localizedPrice` strings; the IAP service substitutes `Product.displayPrice` at runtime. `BundleBadge.label` exposes the human-readable accent string so the engine can stay SwiftUI-free while the app maps to colors.)
* `ChipBundleCatalog.swift` (Session 16 — single source of truth for the five V1 bundles in display order. `all`, `allProductIDs`, `bundle(forID:)` helpers. Product IDs MUST match App Store Connect exactly — see Open Items.)
* `ChipPurchaseProcessor.swift` (Session 16 — pure credit pipeline owning the three IAP business invariants: idempotency (processed-set guard), first-purchase doubler (flag flipped BEFORE credit per force-quit safety pattern), restore-never-doubles. Both production and test-double IAP services route through this so the test double cannot drift.)
* `IAPService.swift` (Session 16 — protocol + `PurchaseResult` + `IAPError` + `InMemoryIAPService` test double. `Sendable`. Test double is configurable per call: scripted `Scripted` enum drives success / userCancelled / pending / verificationFailure / networkError paths. Captures call counters and routes success through `ChipPurchaseProcessor` so test asserts can verify both VM and engine behaviors end-to-end.)
* `StoreKitIAPService.swift` (Session 16 — production. `import StoreKit`. `loadProducts` resolves prices via `Product.products(for:)`. `purchase` walks the StoreKit 2 `Product.PurchaseResult` → `VerificationResult<Transaction>` flow and credits via `ChipPurchaseProcessor` before calling `transaction.finish()`. `restore` calls `AppStore.sync()` then sweeps `Transaction.unfinished` with `isRestore: true`. `startTransactionListener` runs a long-lived `Task` over `Transaction.updates` so purchases that complete outside the active session credit chips on next launch.)
* `TelemetryService.swift` (Session 16 — protocol + `LoggingTelemetryService` (writes to `OSLog` for sandbox debugging) + `RecordingTelemetryService` (in-memory test double). Hooks: `purchase_initiated`, `purchase_succeeded(isFirstPurchase:)`, `purchase_failed(reason:)`, `restore_initiated`, `restore_completed(count:)`. TelemetryDeck wiring is Session 17 — protocol exists now to keep the IAP service from painting itself into a corner.)
* `ChipShopLogic.swift` (Session 16 — pure helpers backing the Chip Shop UI: `bannerText`, `doublerActive(hasMadeFirstPurchase:)`, `displayAmount(for:doublerActive:)`, `strikethroughAmount(for:doublerActive:)`, `formatChipAmount`, `restoreMessage(restoredCount:)`. Used by both the view model and asserted directly in engine tests so doubler math is unit-testable without rendering SwiftUI.)
* `ChipStoreProtocol.hasMadeFirstPurchase` and `ChipStoreProtocol.processedTransactionIDs` (Session 16 — new properties + UserDefaults backing. `processedTransactionIDs` is encoded as `[String]` (sorted on write for stable storage shape) and re-materialized as `Set<String>` on read. Both impls (`UserDefaultsChipStore` / `InMemoryChipStore`) are now `@unchecked Sendable` and the protocol is `Sendable` so the IAP service can mutate the store from a non-main isolation domain.)
* `PersistenceKeys.hasMadeFirstPurchase` and `PersistenceKeys.processedTransactionIDs` (Session 16 — IAP idempotency keys. `reset()` clears them alongside the chip-economy keys.)
* `StackHeight.swift` (Session 17 — public engine enum. Cases `.h1 / .h3 / .h5 / .h10 / .h20` mirror the Phase-1 stack art. `bestFit(for chipCount: Int) -> StackHeight` rounds down to the largest available variant ≤ count and clamps sub-1 counts to `.h1`. Pure integer arithmetic, no SwiftUI. Used by `ChipStackView` via `AssetService.chipStackImage(denomination:height:)`.)

App/Assets.xcassets additions:

* `Cards/` (Session 17 — 53 imagesets: `card_<suit>_<rank>.imageset` for each of 4 suits × 13 ranks plus `card_back.imageset`. Suits use lowercase tokens (`hearts/diamonds/clubs/spades`); ranks use numeric digits (`2`–`10`) and lowercased names (`jack/queen/king/ace`). Each imageset's `Contents.json` declares the expected designer filename; placeholder PNGs (24×24 solid colors per suit) are bundled so the asset compiler doesn't error on missing files. Drop-in path: replace each placeholder PNG with the designer's at the same filename — no Contents.json edits needed.)
* `Chips/` (Session 17 — 5 imagesets: `chip_5 / chip_25 / chip_100 / chip_500 / chip_1000`. Same placeholder + drop-in convention as Cards.)
* `ChipStacks/` (Session 17 — 25 imagesets: `stack_<denomination>_h<height>` for each of 5 denominations × 5 stack heights. Same placeholder + drop-in convention.)
* `tools/generate_placeholder_assets.py` (Session 17 — one-shot generator that created the catalog above. Re-runs overwrite imagesets in place; do NOT re-run after the designer's PNGs land or the real art will be clobbered.)

App/Resources additions (Session 19a):

* `App/Resources/Audio/` — 10 designer-shipped CAF files (`card_deal`, `card_place`, `card_flip`, `chip-_place`, `chips-handle-6`, `chip_payoff`, `fold`, `win_small`, `win_big`, `loss`). Registered in `SixthSeat.xcodeproj/project.pbxproj` as a single folder reference under the App group with `lastKnownFileType = folder; path = Resources/Audio;` so the entire directory copies into the bundle verbatim. Resulting bundle path: `SixthSeatApp.app/Audio/<name>.caf`. Adding a new SFX is now drop-in — copy the CAF into `App/Resources/Audio/`, add a new `SoundEffect` case whose raw value matches the filename (without extension), wire it at the trigger point. The `chip-_place.caf` filename keeps the designer's underscore-after-dash verbatim; we don't rename designer-delivered assets.

App description note:

> `ContentView` is now a `NavigationStack` shell with a `GameDestinationView` wrapper that owns `GameTableViewModel` via `@State`. The PLAY route is now `.tableSelect → .game(tableID:)` — the destination resolves the id back to a `TableConfig` via `TableConfig.table(forID:)` and hands it to the view model. A `.fullScreenCover` over the menu presents `DisclosureModalView` on first launch — the cover's bound `@State` is initialized from `UserDefaults.standard.bool(forKey: PersistenceKeys.hasSeenDisclosure)` at struct init so the modal renders on the very first body pass without a one-frame uncovered menu flash. (Session 16) `SixthSeatApp.init` constructs both `chipStore` and `StoreKitIAPService` and calls `iapService.startTransactionListener()` so the long-lived `Transaction.updates` task is alive for the entire process. `ContentView` carries the `iapService: IAPService` parameter alongside `chipStore`; the `.chipShop` destination instantiates a fresh `ChipShopViewModel` with the shared services. `Configuration.storekit` at the project root + `<StoreKitConfigurationFileReference>` in the iOS scheme lets the simulator transact against local products without an App Store Connect setup or sandbox tester account.

## Workflow Lessons

* Start every session with: `git checkout main && git pull && git log --oneline -5`. Confirm latest commit matches expectation before doing anything else. For bug-fix sessions, also verify the reported bug reproduces locally before fixing — don't fix what isn't broken on the current build.

## Open Items / Housekeeping

**Phone-test pass complete (Session 18a, 2026-05-06).** Walked the full 18-item punch list on iPhone 16 Pro Max running iOS 26.4.2 with a fresh-install build at HEAD `4864148`. Twelve items cleared with no notes, one with a note, three not organically observed (need targeted reproduction next session), and three failed and surfaced new follow-ups. Status of each item below; new follow-ups added as separate sub-sections beneath this list.

1. ⏳ Tier 3 timing at 2400ms — *blocked on phone-test verification. Session 18c shipped the deterministic-deal debug affordance; the included scenarios cover dealer-doesn't-qualify, player flush on river, and push. Add a `straightFlush` (Tier 3) scenario only if a follow-up phone-test pass surfaces it as still-blocking — keeping the menu lean for now per the "three scenarios cover the three ⏳ items" maintenance rule.*
2. ⏳ Royal-flush triple-tap distinctness on hardware — *same as #1; the three preset scenarios cover the three Session 18a ⏳ items but not Tier 3/4 ceremonies. Add a royal-flush scenario when the haptic question becomes the next blocking item; deferred deliberately.*
3. ✅ Fold-loop pacing — Vegas-pace, no jarring frames reported.
4. ✅ Tier 1 flatness fixed — two-pair `+$50` win at $10 minimums read with appropriate weight, not flat.
5. ✅ Main Menu visuals + button readability + nav flow — four-button vertical layout per the 2026-05-04 scope decision; balance reads `$5,000` correctly.
6. ✅ Second-chance bonus flow — **unblocked by Session 18b**. The bust threshold is now table-aware (`GameConstants.minimumPlayableBalance` derives from `TableConfig.cheapestEntryBalance = $60`), so any balance below `$60` triggers the bonus path. New `secondChanceBonusFiresWhenStrandedBetweenStakeMinimums` regression test asserts the `$30` case fires the bonus. On-device re-verification deferred to the next phone-test pass.
7. ✅ Community-card face-down across multiple consecutive hands — verified across 3+ hands at `.table10` and `.table50`.
8. ✅ Casino-feel of the new deal sequence — all 5 community cards visibly arrive face-down at DEAL. (Surface-level perception note: David initially read "no cards" because the placeholder PNGs are 24×24 solid-color squares; once the placeholder convention was understood, the deal sequence read clean.)
9. ✅ First-launch disclosure modal — appeared on first body pass with no uncovered-menu flash; second launch went straight to the menu.
10. ✅ Settings haptics toggle — OFF silenced flips and resolution; ON resumed them.
11. ❌ Settings legal links — **the iOS code is correct (URLs open in Safari)**; the bug is in the served content. `docs/privacy.html` and `docs/terms.html` were exported from TextEdit by Cocoa HTML Writer and contain *escaped HTML/CSS template markup* (`&lt;style&gt;` etc.) as visible text instead of legal copy. App Store submission blocker — see "Legal docs content authoring" below.
12. ✅ How to Play paytables — both paytables render as actual rows (not prose); spot-checked Royal Flush = 500:1 in Blind, 50:1 in Trips.
13. ✅ Table picker layout + tap targets — layout/tap-target/disabled-state behavior all correct, including the Session 15b "no table enterable → Visit Chip Shop fallback button" path (verified at the `$30` stranded balance). C1 currency drift on the `$50` card range fixed in Session 18b — `anteRangeDescription` now routes through `ChipShopLogic.formatChipAmount` and renders `$50 – $1,000`. On-device re-verification deferred to the next phone-test pass.
14. ✅ Persisted last-played table — verified across in-session table changes and across a force-quit + relaunch. After purchasing chips out of the stranded state, the picker correctly re-enabled all three cards above the entry threshold and retained `$50` as `LAST PLAYED`.
15. ✅ Hand-result headline color + legibility — `+$N` rendered green on a two-pair win, `-$N` rendered red on a loss. Push state remains blocked on phone-test verification — the Session 18c `DebugScenario.push` preset (broadway straight on the board, both players play the board) now makes the push headline reproducible on demand from the debug menu.
16. ✅ Table-specific cycle ranges — `.table10` includes `$15` (Session 14b gap closed); `.table25` walks `25 → 50 → 100 → 250 → 500 → 0`; `.table50` walks `50 → 100 → 200 → 500 → 1,000 → 0`; `.table50` Trips floor is `$10`, not `$5`. (In-game `$1,000` displays *with* comma — formatting drift is isolated to the table picker card; see follow-up.)
17. **Chip Shop end-to-end** — broken into sub-items per the Phase 6 walk-through:
    * ✅ 17a — All five tiles render with StoreKit-localized prices (`$0.99 / $1.99 / $4.99 / $9.99 / $19.99`); yellow doubler banner visible; doubled chip counts with strikethroughs; `Most Popular` and `Best Value` badges render on the right tiers; Restore Purchases and Back to Menu visible.
    * ✅ 17b — First purchase (Pocket Change `$0.99`) credited `10,000` chips (doubled), banner disappeared, strikethroughs vanished, no error toast.
    * ✅ 17c — Second purchase (Pocket Change again) credited `5,000` chips (base — doubler stays consumed; `restore-never-doubles` invariant from Session 16 holds at the credit boundary).
    * ✅ 17d — Restore Purchases triggered the native iOS Apple-ID sign-in flow (expected — `AppStore.sync()` requires authentication); button affordance verified as App-Store-review-ready. Real restore-of-real-purchases will get re-tested once the App Store Connect IAP products and a sandbox tester account exist (see "App Store Connect IAP setup").
    * ✅ 17e — Listener-replay safety on hardware: force-quit after the second purchase + relaunch left the balance at exactly `$15,030`, no double-credit, disclosure modal stayed dismissed. The `processedTransactionIDs` set persisted across the launch.
    * ✅ 17f — Forced second-bust → Chip Shop route — **unblocked by Session 18b**. Bust threshold fix opens the path; on-device re-verification deferred to the next phone-test pass.
    * ✅ 17g — Back to Menu from Chip Shop — clean nav-stack return.
18. ✅ Asset pipeline placeholder visuals — **Session 20 landed the Fiverr Phase 1 PNG drop in-place.** All 52 card faces, the card back, 5 chip singles, and 25 chip-stack variants now resolve to real designer art (no more 24×24 placeholder squares); deal sequence still walks face-down → face-up cleanly; per-deal `.id("...")` reuse pattern still flips on the second hand. Initial on-device Debug-build pass on iPhone 16 Pro Max confirmed cards render correctly. Card sizing feels undersized relative to felt area — see Session 20 follow-ups for the UI-polish session candidate.

**Crosscutting C1 currency-display sweep.** `$` format with comma separators and no decimals is the 2026-05-04 convention. Six samples observed this session: `$5,000` (menu), `$50 – $1000` ❌ ($50 table-picker card — drift, fixed in Session 18b → now `$50 – $1,000`), `$4,960` (in-game balance), `$1,000` (in-game `.table50` Ante cycle), `$24,880` (in-game balance), `$15,030` (Chip Shop balance after second purchase). All known drift sites now consistent.

**Crosscutting C2 — eager starter bonus.** Verified: fresh install mounted the menu at `$5,000`, not `$0`. (Session 14a invariant holds.)

**Crosscutting C3 — `hasReceivedSecondChanceBonus` force-quit survival.** ✅ Unblocked by Session 18b (stranded-balance fix opens the bust trigger path). Unit test coverage in `BustFlowTests` continues to cover in-process flag-write ordering. On-device re-verification deferred to the next phone-test pass.

### Phone-test punch list — Session 19a (status post-19b)

The audio system shipped without on-device verification because Claude Code can't drive the simulator audio render thread or test against the device silent switch. **Dealer-showdown SFX item cleared by Session 19b** — what was silent on iPhone 16 Pro Max now fires a single clean `cardFlip` at showdown (per the 19b single-fire-before-await fix; verified on-device by David before the Session 20 asset work). Remaining items still pending the next full audio phone-test pass:

* Verify each of the 10 SFX plays at the right moment (deal × 2, community reveal cards, dealer flip × 1 — **single-fire**, post-19b — , ante/trips cycle taps, Play bet taps, win, loss, fold).
* Verify the **Sound Effects** toggle in Settings silences all SFX when off and re-enables them when on, without an app relaunch.
* Verify the system respects the device silent switch (SFX go quiet when the silent switch is engaged).
* Verify the system mixes politely with background music — start Spotify (or any other audio app), launch 6th Seat, confirm SFX overlay the music without pausing or ducking it audibly.
* Verify no audio glitches / clipping / underruns on rapid plays (e.g. all 5 community cards in `animateAllCommunity`'s 150ms-stagger pre-flop bet sequence).
* Verify no perceptible playback latency — tapping DEAL should fire `cardDeal` instantly when the player card appears, not 100-200ms later. AVAudioPlayer caches lazily on first play of each SFX, so the first session's plays may be slower than steady-state.
* Verify the Settings panel art's third row (Music — designer-shipped art still includes it visually) does not produce an awkward empty gap behind the new two-toggle layout. If it does, log it as a Phase 2 designer follow-up — do not patch the panel art in this session.

### Follow-ups surfaced by Session 18a — status post-18b

* ✅ **Stranded-balance regression — fixed in Session 18b.** `GameConstants.minimumPlayableBalance` now delegates to a new `TableConfig.cheapestEntryBalance` static (currently `$60`). All four call sites (`BonusLogic.applySecondChanceBonusIfEligible`, `GameTableViewModel.maybeShowBustFlash`, `MainMenuLogic.playEnabled`, `MainMenuLogic.handlePlayTap`) keep reading the same constant — single source of truth preserved. New regression test `secondChanceBonusFiresWhenStrandedBetweenStakeMinimums` locks in the table-aware invariant against future stake-level changes. SPEC Drift Log entry added 2026-05-07.
* **Legal docs content authoring — resolved out-of-band before Session 18b.** `docs/privacy.html` and `docs/terms.html` have been replaced with clean Termly export and a clean custom Terms draft, both rendering correctly via GitHub Pages (commit `3930ffb`). No further work needed.
* ✅ **`TableSelectView` currency drift — fixed in Session 18b.** [SixthSeat/Sources/SixthSeat/TableConfiguration.swift:99](SixthSeat/Sources/SixthSeat/TableConfiguration.swift:99) `anteRangeDescription` now routes both ends through `ChipShopLogic.formatChipAmount`, rendering `$50 – $1,000`. `TableConfigurationTests.anteRangeDescriptionIsCorrect` updated to assert the comma-separated form. SPEC Drift Log entry added 2026-05-07.

### Follow-ups surfaced by Session 19b

* **Intermittent `AnimationTests` flakes.** Two existing tests — `Session 12: flop reveal flips [0,1,2] in stagger; [3,4] stay face-down throughout` and `Session 12: bet pre-flop reveal flips all 5 community cards in sequence from face-down` — surfaced as first-run failures during Session 19b and again did not reproduce on the Session 20 test pass. Not 19b regressions (the choreography they assert isn't touched by the dealer SFX fix or the TelemetryDeck wiring) and not 20 regressions (assets don't touch animation timing). Likely a host-load sensitivity in the animation clock — investigate as a standalone reliability follow-up if they start failing more often. Pinning down the root cause is preferable to adding retries.

### Pre-TestFlight Manual Steps

State-of-play for everything that has to happen out-of-band before the first TestFlight build can ship.

* **TelemetryDeck App ID — ✅ done.** Replaced the `"TELEMETRYDECK_APP_ID_PLACEHOLDER"` string with the real UUID from the TelemetryDeck dashboard. Done by David in commit `04bd1b4` immediately after Session 19b. Sanity check: `strings $RELEASE_BINARY | grep TELEMETRYDECK_APP_ID_PLACEHOLDER` should return zero matches; the real UUID should appear in its place.
* **App Store Connect IAP setup (David, manual).** Before the Session 16 IAP code can be exercised in sandbox or TestFlight, the five products must be created in App Store Connect under Features → In-App Purchases → Consumable. For each: set product ID (matching the code's hardcoded IDs exactly — typo = silent failure), price tier, display name, description, English localization. The Session 16 local `Configuration.storekit` lets the simulator run purchases without this — but real sandbox / TestFlight testing requires it. The five product IDs are hardcoded in `SixthSeat/Sources/SixthSeat/ChipBundleCatalog.swift`:

  > `com.sixthseat.uth.chips.pocketchange` ($0.99, 5,000 chips)
  > `com.sixthseat.uth.chips.starter` ($1.99, 25,000 chips)
  > `com.sixthseat.uth.chips.tablestakes` ($4.99, 75,000 chips)
  > `com.sixthseat.uth.chips.highroller` ($9.99, 250,000 chips)
  > `com.sixthseat.uth.chips.deepstack` ($19.99, 750,000 chips)

* **Sandbox tester account (David, manual).** At least one sandbox tester under Users and Access → Sandbox → Testers is required to drive real-purchase flows against the products above on a TestFlight build. Sign out of the production Apple ID on the test device's Settings → App Store → Sandbox Account before testing; sandbox purchases against a production account will silently fail.

### Asset Integration Checklist

Run this on every new designer-delivered art batch BEFORE integration into `App/Assets.xcassets/`. Session 22 surfaced that the Fiverr Phase 1 delivery had two PNGs (`chip_5.png` and `stack_5_h1.png`) at `3661×1909` with the artwork stamped in the corner of a transparent canvas, while every other file in the same set came tight-cropped at `~800×740`. The dimension mismatch caused on-device renders to be ~8px specks where they should have been ~40px chips. The check below catches that class of defect at integration time, not on hardware after merge.

1. **Spot-check pixel dimensions across the delivery.** Run `sips -g pixelWidth -g pixelHeight <file>` on a handful of files in each logical set (cards, chips, chip stacks, button states, popups). Within a logical set the dimensions should be in the same ballpark — order-of-magnitude differences indicate transparent padding or canvas-size export bugs that affect SwiftUI rendering when the asset is sized to a frame.
2. **Compare against the existing asset catalog before overwriting.** `du -h <existing-imageset>/<file>.png` on a few of the current files in the same set. If the new files are dramatically larger or smaller in bytes than the ones you're replacing, eyeball them in Preview before integration.
3. **For any outlier file:** open it in Preview, check the bounding box of visible art vs. the canvas size. If there's significant transparent padding, recrop in place to the tight bbox before integration (`sips --cropToHeightWidth <h> <w>` or a Preview manual crop). Preserves artwork pixel-for-pixel, removes the padding that breaks `.scaledToFit()` framing.
4. **Add an in-test guard for the asset class if it's being used at a fixed frame size.** `ChipArtDimensionsTests` in `AppTests/AssetPipelineTests.swift` asserts aspect ratio is within `[0.7, 1.3]` for every chip-art imageset — catches future oversized-canvas redeliveries from re-introducing the same defect silently. Pattern is reusable for any other asset class where a wide aspect-ratio variation would break the layout.

### Follow-ups surfaced by Session 20

* ✅ **Card sizing on the felt — resolved in Session 21.** Hole cards (player + dealer) scaled 60×84 → 84×118 and community cards 52×72 → 73×101, uniformly 1.4× from the pre-Session-21 baseline. Frame constants live as private `holeCardWidth/Height` + `communityCardWidth/Height` on `GameTableView` and are passed to each `CardView` site explicitly; aspect ratio preserved; Project Convention #4 per-deal `.id("...")` + `Task.yield()` preserved. On-device re-verification deferred to the next phone-test pass — David to confirm the new sizing reads as "casino weight" on iPhone 16 Pro Max.
* ✅ **Bet-zone chip-stack visuals — resolved in Session 21.** `BetZoneView` now renders a `ChipStackView` inside the bet circle whenever the wager is non-zero, routed through the new engine helper `ChipDecomposition.bestFit(for: amount)`. The dollar-amount label moves below the circle and continues to provide the precise wager value when the chip-stack rendering is a visual approximation (off-grid amounts like $35 → one $25 chip). Zero-amount zones render the empty circle + "—" exactly as before. The 2026-05-11 SPEC Drift Log entry on chip-place SFX firing at bet time without a felt visual is now closed — SFX, balance decrement, and chip-stack appearance land on the same beat.
* **Bonus Fiverr UI chrome unused for V1.** The Fiverr delivery included substantial UI chrome beyond Phase 1 cards/chips scope: full iOS app icon set, splash/logo, button states (Default/Disabled/Pressed) for Bet 3x/4x/Check/Deal/Fold/Rebet/Play/Chip Shop/Settings/How to Play/Buy, Chip Shop pack-card variants, badges (Most Popular / Best Value / Last Played), popups (Settings, Payout, Win in three variants), panels, trays, backgrounds (main bg, chip shop bg, table felt). These remain staged in `~/Desktop/fiverr-phase1/` (the in-repo `Design Assets/` and `Final assets - V2/` folders were deleted this session as obsolete pre-delivery copies). Phase 2 integration candidate — but the pattern is different from `AssetService` (button state machines, image-backed view backgrounds) and should be its own scoped session.

### Follow-ups surfaced by Session 21

* ✅ **Broken $5 chip art (`chip_5.png` + `stack_5_h1.png`) — resolved in Session 22.** Both files arrived from Fiverr at `3661×1909` with the chip stamped in the bottom-right of an otherwise-transparent canvas; every other denomination's `chip_<denom>` and `stack_<denom>_h1` came tight-cropped at `~800×740`. The `40×40 .aspectRatio(.fit)` frame in `ChipStackView` scaled the broken art's chip to an ~8px speck on hardware. Recropped both PNGs in place to the chip's visible bbox (`801×746`), preserving the artwork pixel-for-pixel. New `ChipArtDimensionsTests` suite in `AppTests/AssetPipelineTests.swift` loads each `chip_<denom>` and `stack_<denom>_h1` imageset via `UIImage(named:)` and asserts the aspect ratio is within `[0.7, 1.3]` — locks out future oversized-canvas redeliveries from re-introducing the same defect silently.
* ✅ **`ChipDecomposition` lossy single-chip approximation — resolved in Session 22.** Session 21 picked the largest denomination ≤ `amount` and divided regardless of clean fit, so `$50` rendered as `(25, 1)` — visually identical to a `$25` bet. New algorithm requires `amount % D == 0`, so `$50 → (25, 2)` / `$75 → (25, 3)` / `$35 → (5, 7)` (falls back to the largest cleanly-dividing denomination). Off-grid bets that don't divide cleanly into any V1 denomination now fall back to `nil` — but the engine's current chip cycles produce only multiples of `$5`, so the engine never generates such an amount. SPEC's Architectural Decisions entry on `ChipDecomposition` updated to reflect the new exact-decomposition contract.
* ✅ **New Hand carrying over previous bet amounts — resolved in Session 22.** Session 21's `newHand()` cleared `stagedTrips` but left `stagedAnte` at the prior hand's value, so the only way to start a fresh bet was to cycle the Ante zone all the way around to `$0`. `GameTableViewModel.newHand()` now also sets `stagedAnte = 0`; the bet circle returns to "Place your bets" state. Rebet is unchanged — it still restores `lastAnteBet` (and `lastTripsBet`, if affordable). Latent-invariant audit caught one existing test (`AnimationTests.dealIdStableOnNewHand`) that relied on the carry-over behavior; updated.

### Follow-ups surfaced by Session 22

* ✅ **Card-margin uniformity across the sliced card faces — resolved in Session 23.** Phone test of the Session 22 build on iPhone 16 Pro Max surfaced minor visual inconsistency in card-margin placement (one card's corner marker sitting slightly closer to the edge than another's). Investigation showed the card art itself was within 1 px of uniform across the 51 sheet-sliced cards, but `tools/slice_card_sheet.py`'s hardcoded `COL_BOUNDS` and `ROW_BOUNDS` had inter-column gaps varying between 3–6 px; at on-device render scale, even small crop-boundary variance translated to visible margin differences. Slicer rewritten to derive uniform boundaries from the source sheet at runtime via alpha-channel detection + least-squares fit (median card width + linear fit over cleanly-detected card centers; fused runs detected by inflated width and skipped during fit but their grid indices counted). Derived values: `card_width = 125 px`, `card_height = 174 px`, `column_gap = +1.589 px`, `row_gap = +2.050 px`. All 51 sheet-derived cards rewritten with identical 125×174 pixel dimensions; standalone 7D and card back unchanged. Residual error vs the pre-Session-23 hardcoded centers ≤ ±1.5 px on cols, ≤ ±0.5 px on rows. New `--debug` flag on the slicer prints derived values + per-bound residuals for verification. Per-card dimension uniformity is now guaranteed at the script level by construction.

### Follow-ups surfaced by Session 25

* **Unused chip-stack imagesets in `App/Assets.xcassets/ChipStacks/`.** With Session 25's offset-stack rendering, the 20 multi-chip stack imagesets (`stack_<denom>_h<height>` for `h3 / h5 / h10 / h20` × 5 denominations = 20 imagesets) are no longer referenced by any production code path. `BetZoneView` now calls `chipImage(for:)` N times instead of `chipStackImage(denomination:height:)` once. The remaining 5 `stack_<denom>_h1` variants are still loaded by `ChipArtDimensionsTests`'s aspect-ratio guard — they can be migrated to read from `chip_<denom>` instead (the `h1` art is the same as the single-chip art) and the imagesets deleted alongside the multi-chip variants. `StackHeight` enum and `AssetService.chipStackImage(denomination:height:)` are also now unused except in their own self-tests (`StackHeightTests`, `AssetNamesTests.stackName`, `InMemoryAssetServiceTests.recordsStackRequests`). Defer cleanup to a focused housekeeping session — small, low-risk, but distinct from the bet-zone rendering work and worth doing in isolation so the unused-code removal is easy to review.

### Follow-ups surfaced by Session 23

* ✅ **Cards still look clipped at the bottom (per-card art shift inside uniform crops) — resolved in Session 24.** Photoshop verification on the source sheet (shared in chat after the Session 23 build hit hardware) showed the designer's cards sit at slightly different vertical positions within their otherwise-uniform grid cells. Session 23's uniform-pitch crops therefore landed each card's art at a different vertical offset inside a pixel-identical PNG — read on-device as some cards' bottoms clipped, others padded with extra transparency. Session 24 keeps the Session 23 uniform grid as a *search region* but adds PIL `getbbox()`-based per-card tight-bbox detection inside each cell, then pads each tight crop to a uniform output canvas (`max(detected_w) × max(detected_h)`) with the art centered and transparent margins added on all four sides. Search margin is held at `0` because the source's inter-card gap is ~1–2 px (rows 6–7 alpha-touch) — any margin > the gap would let neighbors bleed into the search region. Detected bboxes ranged `123–125 × 171–174` px (avg `124.06 × 172.80`); target canvas resolved to `125 × 174` (identical pixel dimensions to Session 23 output, but each card's art is now centered to ≤1 px tolerance regardless of where it sat in the source sheet). Sanity gate aborts the run before any write if any bbox dim differs from the Session-23 uniform nominal by > 50%. All 51 cards passed. 7D SHA verified unchanged before/after. The `LAYOUT` dict, the dup-AD skip, and the missing-7D handling are untouched.

**Deferred (asset-blocked or later session):**

* ✅ **Chip balance updates immediately on bet placement — resolved in Session 21.** The original concern was that the balance number would drop with nothing on the felt to show where the chips went. With bet-zone chip stacks now landing inside the circle on every non-zero wager (`BetZoneView` reads `ChipDecomposition.bestFit(for: amount)` and renders `ChipStackView`), the balance decrement is now visually consistent with chips having physically moved onto the table. Tracked here through the post-Session-12 → Session 21 arc; closed.
* **Mid-game table switching (out for V1).** No in-game "change table" affordance — the player must back out to the menu and re-pick. Considered out of scope for V1 because mid-game switching needs to interleave with bet/deal/resolve. Revisit if hardware feedback says the menu round-trip feels heavy.
* **Trips-win legibility on the headline (revisit only if it surfaces).** Session 15c collapses the 15b per-bet breakdown back into a single signed net-result number. Trips wins lose their dedicated callout — the player sees the combined number rather than the trips paytable hit. Accepted tradeoff because the breakdown read as too busy on a phone screen. If hardware testing later surfaces "I don't know my Trips paid" as a real player-feel issue, address with a lighter-touch solution (a small per-side-bet badge or accent on the headline) rather than re-adopting the full breakdown.
* **Table-specific visual themes (V2).** All three tables share the same green felt and accent color in V1. Different felt tones / accent colors / chip art per table is a V2 polish opportunity that depends on the Fiverr asset drop landing first.
* **Variable starting bankroll based on table choice (V2).** Player gets the same 2,500-chip starter regardless of which table they picked first. Real-money rooms scale the buy-in to the table — V2 candidate.
* **Mid-game Settings access (Session 15a deferred).** No gear icon on the game table in V1. Players reach Settings only via the Main Menu Back path. Acceptable for V1 because in-game Settings would need its own modal/pause flow and would interleave with the bet/deal/resolve sequence. Revisit when Session 19a audio shipping + on-device testing tells us whether players want a quick mute during a hand.

## What's Next

* **Session 12 — done.** Reversed from "struck" after 2026-04-27 phone test surfaced casino-realism gap (community cards animating in face-down at phase, instead of being pitched out at hand start).
* **Session 14 — done.**
* **Session 14a — done.**
* **Session 14c — done.** Dealer hole cards now carry the `.id("dealer-card-\(currentDealId)-N")` modifier and `animateDealerHoleCards` opens with `await Task.yield()`, completing Project Convention #4 across all card slots.
* **Session 12a — done.** Ante bet zone now uses tap-to-cycle ($5 → $25 → $100 → $500 → $1,000 → $0) mirroring the Trips zone. Removed the +/- stepper UI and the `incrementStagedAnte` / `decrementStagedAnte` / `anteSteps` model surface entirely. Blind continues to mirror Ante automatically (engine invariant in `placeAnte`), and DEAL is now disabled when the cycle lands on $0.
* **Session 12b — done.** Bust detection moved in-game. After chip resolution lands the balance at zero, a brand-voiced flash modal fires: first bust awards 2,500 chips with a `.success` haptic and resets the table to `.awaitingBets` with Ante = $5 behind the modal; second bust uses a `.warning` haptic and routes to the Chip Shop via path replacement (`path = [.chipShop]`). The `hasReceivedSecondChanceBonus` flag is set at the moment of award, *before* the modal is shown, so a force-quit during the modal cannot replay the bonus. Chip Shop stub upgraded with title, "Chip bundles coming soon." line, and a Back to Menu button. The Session 14 menu-boundary check stays in place as a fallback.
* **Session 12c — done.** Doc cleanup. The Architectural Decisions section was removed from `HANDOFF.md` (the prior 12b entry already lives in `SPEC.md`); per project convention, durable decisions live in `SPEC.md` and `HANDOFF.md` is operational state only. The latent-invariant audit pattern (recurring across Sessions 12, 12a, 12b) was promoted to a formal Workflow Lesson in `SPEC.md`. No code or test changes; test count remains 267.
* **Session 12d — done.** Affordability gates and bust threshold correctness. The bust trigger now fires when `chipBalance < GameConstants.minimumPlayableBalance` (= 2 × minimum chip value = $10), not just at exact zero — a player who lands at $5 after a fold can no longer be stranded. The DEAL button is gated on `chipBalance >= 6 × stagedAnte` (worst-case Ante + Blind + 4× pre-flop Play); below that the button greys out and the player cycles Ante down to find an affordable value. Trips is force-cleared and disabled when balance covers the worst-case main bet but not Trips on top, and re-enables (without auto-restoring a prior amount) when the player cycles Ante down enough. The Session 14 menu-boundary fallback uses the same threshold. New `GameConstants` enum in the engine package centralizes the minimum chip value and the playable threshold. Test count: 282 (124 engine + 158 app, +15 from Session 12c).
* **Session 15a — done.** Submission-ready surfaces for App Store review. Adds the first-launch Apple 4.3 disclosure modal (entertainment-only language, single "I Understand" button, non-dismissible by background tap, persists `PersistenceKeys.hasSeenDisclosure`). Replaces the Settings stub with a real Form-based screen (SFX/ambient/haptics toggles; legal section with informational copy and links to the hosted privacy policy + terms of service GitHub Pages docs from Session 12-prep; About section with bundle version). Replaces the How to Play stub with a single scrollable, sectioned reference whose two paytables source rows directly from `UTHRules.blindPaytable` and `UTHRules.tripsPaytable` — engine and UI cannot drift. New `GatedHapticsService` wraps any `HapticsService` and gates immediately on `PersistenceKeys.settingsHapticsEnabled` (default-on, read at the call site). Chip Shop stub gains a no-cash-value reinforcement line above the existing copy. Test count: 311 (124 engine + 187 app, +29 from Session 12d).
* **Session 15b — done.** Resolves the four deferred items called out at the end of Session 15a. (1) Adds `TableConfig` to the engine — `Equatable, Hashable, Codable, Identifiable, Sendable` — with three V1 tables (`.table10` / `.table25` / `.table50`) carrying their own `anteCycle` / `tripsCycle` / `minimumAnte`. The view model layer reads cycles off a `TableConfig` instance instead of inline literals; `GameTableViewModel.init` accepts a `TableConfig` (default `.table10`) and the Trips affordability gate now reads `tableConfig.minimumTripsStep`. (2) Adds `TableSelectView` between PLAY and the game; the route becomes `MainMenu → .tableSelect → .game(tableID:)`. The screen renders three cards with affordability gating (cards disable when `chipBalance < 6 × minimumAnte`), persists the chosen id under `PersistenceKeys.selectedTableID` via `@AppStorage`, and falls back to a Chip Shop button when no table is enterable. (3) **Shipped and reverted in 15c — see the 15c entry below.** (4) The `anteCycle` / `tripsCycle` literals in `GameTableViewModel.swift` are gone — both flow off `tableConfig` now. Test count: 361 (138 engine + 223 app, +50 from Session 15a). Per the latent-invariant Workflow Lesson, the cycle-walking and DEAL-gate boundary tests in `GameTableViewModelTests` were rewritten to track the new `.table10` defaults, and bust-flow tests now read the table minimum from `TableConfig.defaultTable.minimumAnte` rather than the old hardcoded `5`.
* **Session 15c — done.** Reverts the per-bet payout breakdown shipped in Session 15b after a partial hardware pass: the multi-line breakdown read as too busy on a phone screen. The `.handComplete` headline goes back to a single signed `+$N` / `-$N` / `Push` number, colored green / red / white off `HandResultHeadline.tone(for:)`. `HandResultHeadline` is a small pure helper at the bottom of `App/Views/GameTableView.swift` — a `Tone` enum and `tone(for:)` / `text(for:)` mappings — so the color/text behavior can be asserted from tests without rendering SwiftUI. Source of truth remains the engine's `BetResolution.HandResult.totalNet`; no engine changes. `App/Views/PayoutBreakdownView.swift` and `AppTests/PayoutBreakdownTests.swift` are deleted; the four pbxproj references are pruned. New `AppTests/HandResultHeadlineTests.swift` covers tone (win/loss/neutral), text formatting, and confirms the headline reads `result.totalNet` without recomputing it. Trips wins lose their dedicated callout in this version — accepted UX tradeoff, captured as a deferred item to revisit only if hardware feedback flags it. Test count: 351 (138 engine + 213 app, −10 from Session 15b: −15 breakdown tests, +5 headline tests).
* **Session 16 — done.** Real Chip Shop with StoreKit 2 IAP. Five consumable tiers (`pocketchange` $0.99 / `starter` $1.99 / `tablestakes` $4.99 / `highroller` $9.99 / `deepstack` $19.99), per-install first-purchase doubler (`hasMadeFirstPurchase` flag flipped *before* credit per the force-quit-safety pattern from Session 12b), idempotent crediting (persisted `processedTransactionIDs` set as the load-bearing defense against listener replay / restore re-emission / Family Sharing redelivery), `Transaction.updates` listener started at app launch, Restore Purchases affordance, telemetry stubs (no-op `LoggingTelemetryService` for now; TelemetryDeck wiring deferred). New engine files: `ChipBundle`, `ChipBundleCatalog`, `ChipPurchaseProcessor`, `IAPService` + `InMemoryIAPService`, `StoreKitIAPService`, `TelemetryService` + `LoggingTelemetryService` + `RecordingTelemetryService`, `ChipShopLogic`. New app file: `ChipShopViewModel` (`@Observable @MainActor`). `ChipShopView` rewritten with the real screen. New project file: `Configuration.storekit` at root, wired into the iOS scheme via `<StoreKitConfigurationFileReference>`. `SixthSeatApp.init` constructs the IAP service and starts the listener; `ContentView` carries `iapService: IAPService` and instantiates a fresh `ChipShopViewModel` on each `.chipShop` route. Test count: 411 (179 engine + 232 app, +60 from Session 15c — 41 engine + 19 app). Per the latent-invariant Workflow Lesson: existing `ChipStoreTests` updated for the new keys + `reset()`; existing `ChipShopView()` instantiations in `MainMenuViewTests` and `BustFlowTests` updated for the new view model param; `ChipStoreProtocol` becomes `Sendable` so the IAP service can mutate the store from a non-main isolation domain.
* **Session 17 — done.** Asset pipeline scaffolding. Goal: when the Fiverr Phase 1 PNGs land in 2-3 days, swapping `placeholder_*.png` for `card_hearts_ace.png` (etc.) is the *only* work needed for visual integration. Built three layers: (1) The asset catalog itself — 83 imagesets (`Cards/`, `Chips/`, `ChipStacks/`) with `Contents.json` files declaring the designer's expected filenames and 24×24 solid-color placeholder PNGs at those filenames so the catalog compiles without errors. Generator script at `.claude/scripts/generate_placeholder_assets.py` is one-shot, do not re-run post-designer-drop. (2) `AssetService` protocol + `BundleAssetService` (production, named-image lookup) + `InMemoryAssetService` (test double, records requests and returns SF Symbol placeholders) + `AssetNames` (pure name-mapping helper, asserted directly in `AssetNamesTests`). All injected via the SwiftUI `\.assets` environment value. (3) View refactors — `CardView` switched from `faceDown: Bool` to `faceUp: Bool` and now renders `assets.cardImage(for:)` / `assets.cardBack()` resized inside its existing flip animation; `ChipStackView` switched from `amount: Int` to `(denomination: Int, count: Int)` and routes through `StackHeight.bestFit(for: count)`; new `ChipView(denomination:)` renders single chips. `StackHeight` lives in the engine package because `bestFit(for:)` is pure integer arithmetic worth covering with engine tests. `GameTableView` call sites negate `viewModel.isXCardFaceDown(index:)` at the boundary so the view model surface is unchanged. Test count: 433 (184 engine + 249 app, +22 from Session 16: +5 engine for `StackHeight`, +17 app for `AssetNames` / `InMemoryAssetService` / `CardView` rendering / `ChipView`+`ChipStackView` rendering). Per project convention #4 the per-deal `.id("...")` modifiers and `Task.yield()` in animation entry points are preserved on every `CardView` slot.
* **Session 18a — done.** Phone-test pass against the cumulative Sessions 11/12/14/14a/15a/15b/15c/16/17 backlog. iPhone 16 Pro Max on iOS 26.4.2, fresh install built from HEAD `4864148`. No new feature code; no SPEC architectural changes. Walked the full 18-item punch list plus crosscutting C1/C2/C3 sweeps — see "Phone-test pass complete" above for per-item status. Cleared 12 items + 17a–17e/g (Chip Shop end-to-end), one warned (Item 13 — currency drift on the table-picker `$50` card), three not organically observed (Tier 3 ceremony, royal-flush triple-tap, push-state headline — all need targeted reproduction in a follow-up session, ideally backed by a deterministic-deal debug affordance), and three blocked by a newly-surfaced **stranded-balance regression** that traces back to Session 15b. The blocker prevents reaching the bust trigger from any balance in the gap `$10 ≤ balance < $60`, which in turn blocks the second-chance-bonus, force-quit-survival, and forced-second-bust → Chip Shop verifications (Items 6 / C3 / 17f). Test count unchanged at 433 (184 engine + 249 app). All three new follow-ups are documented in Open Items above with priority ordering: stranded-balance fix (highest, gameplay-correctness), legal-docs content (App-Store-blocking), table-picker currency drift (low, easy win).
* **Session 18b — done.** Bug-fix pass driven by 18a's three findings (one of which — the legal docs — was resolved out-of-band by David before this session, so it didn't need code work here). (1) **Stranded-balance fix.** Added `TableConfig.cheapestEntryBalance` static helper (currently `$60`, the minimum across all tables' `minimumEntryBalance`); repurposed `GameConstants.minimumPlayableBalance` to delegate to it. All four sites consume the same constant — single source of truth maintained, no signature changes. Added `secondChanceBonusFiresWhenStrandedBetweenStakeMinimums` as a regression test that asserts the bonus fires from `$30` with the V1 tables and will fail loudly if the threshold ever stops tracking `TableConfig`. Recalibrated the existing `BustFlowTests.bustDoesNotFireAtThreshold` scenario for the new `$60` boundary; updated `GameConstantsTests.minimumPlayableBalanceTracksCheapestTableEntry` to assert the new derivation. (2) **Currency drift fix.** `TableConfig.anteRangeDescription` now routes both range ends through `ChipShopLogic.formatChipAmount`, so the `$50` card renders `$50 – $1,000` (was `$50 – $1000`). `TableConfigurationTests.anteRangeDescriptionIsCorrect` updated to assert the comma-separated form. Both fixes documented in the SPEC's Resolved Scope Drift Log per the latent-invariant Workflow Lesson — Session 15b's stake-level expansion silently invalidated a previously-correct global threshold; the log entry IS the lesson that prevents recurrence. Items 6, 13, 17f, and crosscutting C3 unblocked for the next phone-test pass. Test count: 434 (185 engine + 249 app, +1 from Session 17 — the new regression test).
* **Session 18c — done.** Added a DEBUG-only deterministic deal-forcing affordance so the three ⏳ items from Session 18a (dealer doesn't qualify, community-board sequence, push outcome) can be reproduced deterministically rather than waited for. Three layers: `Deck(forcedDealOrder:)` engine initializer (DEBUG-only), `GameState.setForcedDeck(_:)` swap method (DEBUG-only), `DebugScenario` enum + `DebugDealForcer.pendingScenario` static buffer carrying three preset 9-card sequences (DEBUG-only, in the engine package). Hidden UI: a 1.5-second long-press on the Main Menu title presents a `DebugMenuView` sheet whose only visible affordances are scenario buttons + a Clear option. `GameTableViewModel.deal()` consumes and clears the buffer before dispatching the engine deal, so the force is single-shot. All three layers (engine types, view model wiring, view file, and `MainMenuView` gesture) are wrapped in `#if DEBUG`. Verified empty in the Release `SixthSeatApp` binary — `strings` returns zero hits for any of the new identifiers, while the same identifiers all appear in the Debug build's `SixthSeatApp.debug.dylib`. Five new engine tests under a `DebugDealForcer (DEBUG-only)` suite covering `Deck(forcedDealOrder:)` order, `GameState.setForcedDeck` deal-sequence assertion, and one outcome assertion per scenario. Test count: 439 (190 engine + 249 app, +5 from Session 18b).
* **Session 19a — done.** Audio system foundation. New App-layer `AudioService` protocol with `AVAudioService` (production, cached `AVAudioPlayer` per `SoundEffect`, `AVAudioSession` configured for `.ambient` + `.mixWithOthers` on iOS) and `InMemoryAudioService` (test double, records each `play(_:)`). 10 designer-shipped CAF files land at `App/Resources/Audio/` as a folder reference; the bundle ships them at `SixthSeatApp.app/Audio/*.caf` so adding a new SFX is now drop-in. Trigger map wired into `GameTableViewModel`: player hole-card reveal → `.cardDeal`; community reveal → `.cardPlace`; dealer showdown → `.cardFlip`; Ante/Trips cycle → `.chipPlace`; Play bets → `.chipStackHandle`; fold → `.fold`; flush-or-better wins → `.winBig` + `.chipPayoff`; sub-flush wins → `.winSmall` + `.chipPayoff`; losses → `.loss`; pushes → silent. Music toggle removed from `SettingsView` along with `PersistenceKeys.settingsAmbientEnabled` (no music asset sourced for V1; toggle would be non-functional). SPEC's Resolved Scope Drift Log entry dated 2026-05-11 records the removal. Two commits as required (audio foundation + Music removal). Test count: 448 (190 engine + 258 app, +9 from Session 18c — 3 AudioServiceTests + 6 SFX-wiring assertions in a new `GameTableViewModelAudioTests` suite). On-device verification is the next phone-test punch list.
* **Session 19b — done.** Two-part session: dealer-showdown SFX fix from the 19a phone test + the long-deferred TelemetryDeck wiring from Session 18. **Part 1 — Audio fix.** Dealer hole cards were silent at showdown reveal on iPhone 16 Pro Max despite `animateDealerHoleCards` wiring `audio.play(.cardFlip)` for each of the two dealer cards. Root cause: the per-card pattern called `currentTime = 0; play()` on the SAME cached `AVAudioPlayer` 200ms apart, but `card_flip.caf` is 0.92s long, so the second call restarted the first instance mid-playback and the audible result on hardware was silence. Fix lands in two places: `animateDealerHoleCards` now fires `.cardFlip` exactly once, synchronously, BEFORE the first `await Task.yield()` — single audible beat, no same-player restart, and the SFX lands ahead of any await so a same-frame skip-to-settled still produces audio. `finalizeSettledState` fires `.cardFlip` once as a defense-in-depth fallback when dealer cards are being newly-revealed (catches `bypassAnimation` test paths and skip-mid-dealer-reveal user paths) — guarded against double-firing by checking `revealedCards` first. Existing `Showdown fires cardFlip × 2` test recalibrated to `× 1` (single-fire contract); two new regression tests in `GameTableViewModelAudioTests` pin the fallback path and the no-double-fire invariant. **Part 2 — TelemetryDeck integration.** Pulled `TelemetryDeck/SwiftSDK` via the local `SixthSeat` Swift package (resolved at 2.12.0). `TelemetryDeckTelemetryService` (engine package) conforms to the existing `TelemetryService` protocol; one new method `handResolved(tableID:anteAmount:tripsAmount:resultTone:tripsOutcome:)` plus `HandResultTone` and `TripsTelemetryOutcome` enums added to the protocol. `SixthSeatApp.init` runs `TelemetryDeck.initialize(config:)` BEFORE `iap.startTransactionListener()` so background `Transaction.updates` deliveries always dispatch to a live SDK; DEBUG builds keep `LoggingTelemetryService` so the dev loop doesn't burn TelemetryDeck quota with noise. App ID is the hardcoded placeholder `"TELEMETRYDECK_APP_ID_PLACEHOLDER"` with a `// TODO: David — set TelemetryDeck app ID before TestFlight` comment; verified to ship in the Release binary via `strings`. Hand telemetry fires from `syncFromGame` on the `.handComplete` transition (same beat as `lastAnteBet` snapshotting). Payload schema: IAP succeeded carries product_id, base_chip_amount (derived from `ChipBundleCatalog`), doublered_chip_amount, doubler_fired, is_first_purchase; hand resolved carries table_id, ante_amount, trips_amount, result_tone, trips_outcome. Balance amounts NOT logged (privacy). Tests: 8 engine tests in a new `TelemetryDeckTelemetryService payload shape` suite assert signal-name + parameter-dict contract via testable static helpers; 3 app tests in a new `GameTableViewModelTelemetryTests` suite assert call-site shape via `RecordingTelemetryService`. SPEC's Architectural Decisions gains two new entries (dealer SFX single-fire + TelemetryDeck integration); Drift Log gains the 2026-05-11 chip-place-SFX-at-bet-time entry. Two commits as required (audio fix + TelemetryDeck). Test count: 461 (198 engine + 263 app, +13 from Session 19a — 8 engine + 5 app).
* **Session 20 — done.** Fiverr Phase 1 asset integration. Sliced 51 card faces from the designer-delivered sheet via the new `tools/slice_card_sheet.py`; David manually placed the standalone 7-of-Diamonds (sheet bug: duplicate Ace of Diamonds occupied the 7D slot) and the card back. Chips: 5 singles + 25 stack variants from `Vector illustrations/Coins/`. All 83 asset-catalog slots that Session 17 scaffolded are now populated with real designer art — drop-in worked as designed, zero view code changes. On-device Debug-build verification on iPhone 16 Pro Max confirmed cards render correctly (engine math, hand resolution, payout headlines all functional with the new art). Chips not yet surfacing on the game table — expected, bet-zone chip-stack visuals are still a view-code deferred item (see Follow-ups surfaced by Session 20). Test count unchanged at 461 (198 engine + 263 app); assets don't touch engine or app logic. Tooling: `tools/slice_card_sheet.py` kept in-repo for any future re-slice; `.gitignore` updated for `SixthSeat.xcodeproj/project.xcworkspace/xcshareddata/` and `.claude/settings.local.json`; obsolete pre-delivery `Design Assets/` and `Final assets - V2/` folders deleted. Two commits as required (tooling/gitignore + asset integration & documentation backfill of Session 19b supplements).
* **Session 21 — done.** UI polish driven by Session 20 phone-test gaps. (1) Bet-zone chip stacks: new engine helper `ChipDecomposition.bestFit(for:)` returns `(denomination, count)` using the largest available denomination ≤ amount; `BetZoneView` rewritten to render label-above / chip-stack-inside-circle / amount-below; chip stack scales/slides/fades with the rest of the zone during chip-resolution animation. Zero-bet zones still render the empty circle with "—". The 2026-05-11 SPEC Drift Log entry on chip-place SFX phantom-feel is now closed — SFX + balance decrement + chip-stack appearance fire on the same beat. (2) Card sizing 1.4× uniformly: hole 60×84 → 84×118, community 52×72 → 73×101; same scale across all three slots; Project Convention #4 (per-deal `.id("...")` + `Task.yield()`) preserved at every CardView site. Test count: 472 (206 engine + 266 app, +11 from Session 20 — 8 engine `ChipDecompositionTests` + 3 app `BetZoneViewChipStackTests`). Latent-invariant audit cleared — no existing test asserts `BetZoneView` internal structure or card frame dimensions. Debug build for iOS simulator succeeded; full `xcodebuild test` pass on iPhone 17 Pro Max simulator. The simulator first-launch disclosure modal renders correctly; programmatic tap-through to capture the game-table screen was not available this session (no UI test target, no `simctl tap` primitive) so the chip-stack-on-felt visual is for David's on-device pass to verify. Two commits as required (engine helper + tests; view code + asset wiring + docs).
* **Session 22 — done.** Three-fix UI pack driven by the Session 21 phone test on iPhone 16 Pro Max. (1) **Broken $5 chip art recropped.** Fiverr delivered `chip_5.png` and `stack_5_h1.png` at `3661×1909` with the chip stamped in the bottom-right of an otherwise-transparent canvas; every other denomination came tight-cropped at `~800×740`. Cropped both PNGs in place to their visible bbox (`801×746`), preserving the chip artwork pixel-for-pixel. New `ChipArtDimensionsTests` suite in `AppTests/AssetPipelineTests.swift` loads each `chip_<denom>` and `stack_<denom>_h1` imageset and asserts the aspect ratio is within `[0.7, 1.3]` — locks out any future oversized-canvas redelivery silently re-introducing the defect. (2) **Multi-chip decomposition.** Session 21's `ChipDecomposition.bestFit` returned the largest denomination ≤ `amount` and divided regardless of clean fit (so `$50 → (25, 1)` — visually identical to a `$25` bet). New algorithm requires `amount % D == 0`, yielding `$50 → (25, 2)` / `$75 → (25, 3)` / `$1500 → (500, 3)` / `$35 → (5, 7)` (falls back to the largest cleanly-dividing denomination). Single-character change; docstring rewritten. Existing tests recalibrated to the new contract; new `multiChipDecompositionsAreVisuallyDistinct` pins five representative amounts. (3) **New Hand clears staged Ante.** `GameTableViewModel.newHand()` now sets `stagedAnte = 0` alongside the existing `stagedTrips = 0`, so the bet circle returns to "Place your bets" state instead of carrying over the previous hand's bet. Rebet path unchanged. New `newHandClearsStagedAnte` test in `GameTableViewModelTests`; one existing test (`AnimationTests.dealIdStableOnNewHand`) updated to re-stage Ante before its second deal (latent invariant from Session 21). Test count: 476 (207 engine + 269 app, +4 from Session 21 — 1 engine + 3 app). Full `xcodebuild test` pass on iPhone 17 Pro Max simulator. SPEC's Architectural Decisions entry on `ChipDecomposition` updated to reflect the new exact-decomposition contract. Two commits as required (engine + tests; views + asset crops + docs).
* **Session 23 — done.** Card-sheet reslice with programmatically uniform crop boundaries. Direct response to the Session 22 phone test on iPhone 16 Pro Max, which surfaced minor visual inconsistency in card-margin placement (one card's corner marker sitting slightly closer to the edge than another's). Investigation traced the variance to `tools/slice_card_sheet.py`'s hardcoded `COL_BOUNDS` / `ROW_BOUNDS` having inter-column gaps varying between 3–6 px; the card art itself was within 1 px of uniform. Slicer rewritten to derive uniform boundaries at runtime via alpha-channel detection + least-squares fit over the cleanly-detected card centers; fused runs (rows 6–7 alpha-touch on this sheet) detected by inflated width and skipped during the fit but their grid indices counted via `round(width / median_width)` so projection covers them. Derived values: `card_width = 125 px`, `card_height = 174 px`, `column_gap = +1.589 px`, `row_gap = +2.050 px`. Residual error vs the pre-Session-23 hardcoded centers ≤ ±1.5 px on cols, ≤ ±0.5 px on rows — well within the plan's 2 px tolerance. All 51 sheet-derived cards now share identical 125×174 pixel dimensions, verified by `sips`. New `--debug` flag prints derived values + per-bound residuals. The `LAYOUT` dict, the duplicate-Ace-of-Diamonds skip, and the standalone 7D (`card_diamonds_7.imageset/card_diamonds_7.png`, David-sourced replacement) are untouched — slicer's pre-existing "no sheet position maps to ('diamonds', '7')" handling preserves 7D; SHA verified before/after. Card back re-saved byte-identical (PIL re-encode with `optimize=True` on the same source produced identical bytes). Test count: 476 (207 engine + 269 app, unchanged from Session 22). Per-card uniformity is now guaranteed at the script level by construction — a `CardArtDimensionsTests` suite was considered and skipped (script-level guarantee is the right place). One commit (slicer change + resliced PNGs tightly coupled — separating them creates an in-between state where the script doesn't match the asset catalog). No SPEC change.
* **Session 24 — done.** Per-card bbox detection + uniform-canvas padding in `tools/slice_card_sheet.py`. Direct response to the Session 23 follow-up surfaced in chat: Photoshop verification of the source sheet showed cards sit at slightly different vertical positions within their grid cells, so Session 23's uniform-pitch crops landed each card's art at a different vertical offset inside an otherwise pixel-identical output PNG (read on-device as some bottoms clipped, others padded). Slicer keeps the Session 23 alpha-derived uniform grid as a *search region* and now runs PIL `Image.split()[-1].getbbox()` inside each cell to detect the card art's tight opaque bbox per-card; tight crops are then padded to a single uniform output canvas (`max(detected_w) × max(detected_h)` across all 51 cards) with the art centered and transparent margins added on all four sides. Search margin held at `0` because the source's inter-card gap is ~1–2 px and any margin > the gap pulls neighbor art into the search region (empirically verified — margin=20 produced 165×214 bboxes contaminated by neighbors). With margin=0 the bbox detection finds tight extents `123–125 × 171–174` (avg `124.06 × 172.80`); target canvas resolves to `125 × 174` — same pixel dimensions as Session 23 output, but each card's art is now centered to ≤1 px tolerance independent of its source-sheet offset. Sanity gate aborts the run with `exit 2` before any PNG write if any detected bbox dim differs from the Session-23 uniform nominal by > 50% (`BBOX_SANITY_FRACTION`). All 51 bboxes passed; all 51 centers within 5 px of nominal. 7D SHA preserved (`40766b47c3a0d0b450039b573ba7851fa82f7d27` before and after). Card back re-saved byte-identical (same single-source file as Session 23). The `LAYOUT` dict, the dup-AD skip, and the missing-7D handling are untouched. Test count: 476 (207 engine + 269 app, unchanged from Session 23). One commit (slicer change + resliced PNGs + docs together — separating them creates an in-between state where the script doesn't match the asset catalog). SPEC's Architectural Decisions section gains a one-line entry noting that card art is padded to a uniform canvas at slice time, not at render time.
* **Session 25 — done.** Bet-zone chip rendering rebuilt around the casino-canonical mixed-denomination physically-stacked visual. (1) `ChipDecomposition` redesigned: `decompose(amount:) -> [ChipChunk]` greedy largest-first, returning chunks ordered largest to smallest (`$125 → [($100,1), ($25,1)]` / `$1235 → [($1000,1), ($100,2), ($25,1), ($5,2)]`). Old `bestFit(for:)` deleted; every call site moved to `decompose(amount:)`. (2) `ChipStackView` rewritten: takes `[ChipChunk]`, renders each chunk as N copies of single-chip art in a `ZStack(alignment: .bottom)` with each chip offset upward by `perChipOffset = 8` pt (~20% of `chipDiameter = 40` pt). Largest denomination sits on the bottom. (3) `BetZoneView` switched to the new contract; the SwiftUI external surface (label/amount/animation/onTap) is unchanged so the four `GameTableView` instantiations need no edits. **Asset-catalog impact.** The 20 multi-chip `stack_<denom>_h<height>` imagesets (h3/h5/h10/h20 × 5 denominations) are now unreferenced by production code — marked for cleanup in a focused housekeeping session. The `h1` variants are still loaded by `ChipArtDimensionsTests`. (4) **Latent-invariant audit** clear: only one production call site (`BetZoneView`); no telemetry tests touch chip rendering; the 2026-05-11 chip-place SFX Drift Log entry stays `✅ in build [Session 21]` because SFX still fires at bet placement and chip visual still appears at the same beat. **Chip offset value chosen.** `perChipOffset = 8` pt against `chipDiameter = 40` pt. Worst-case V1 stack is 5 chips (`$750` or `$1235`) → visual stack height `40 + 4×8 = 72` pt, which fits comfortably against the 60-pt bet circle (overflows by 12 pt total, centered around the circle's middle when the chip stack is anchored to the bottom — the top chip rises slightly into the label-above area, the bottom chip sits at the bottom of the circle). 6+ chip bets would overflow further but no V1 chip cycle / Play raise combination produces a 6-chip bet. Tests: 479 (207 engine + 272 app, +3 from Session 24 — engine net zero, app +3). New `expectChipRequestPattern` test helper tolerates SwiftUI's multi-pass `ForEach` evaluation under `ImageRenderer`. Full simulator test pass + simulator build succeeded.
* **Next firm step.** Phone-test pass on iPhone 16 Pro Max focused on the new chip rendering: (a) Session 25 specifically — bet zones with $10/$15/$30/$125/$1235 render the right mixed-chip stacks with the rim-overlap visual; the bottom chip is the largest denomination; the stack scales/slides/fades coherently during resolution motion; (b) carry-over Session 22 items not yet on hardware — New Hand returns bet circles to "Place your bets"; Rebet still carries prior bet forward; (c) carry-over Session 23/24 — card-margin placement uniform and each card's art centered within its uniform canvas; (d) carry-over Session 19b/20 — single clean cardFlip on every showdown; no audio glitches on rapid DEAL/CHECK/BET; sandbox IAP completes land `iap.purchase.succeeded` payloads in the TelemetryDeck dashboard. After that pass, **TestFlight prep** — App Store Connect IAP product creation, sandbox tester account setup, Release-build verification pass against the pre-TestFlight manual-steps checklist. The unused-stack-art-imagesets housekeeping cleanup is a separate small follow-up.

## Known Gaps and Tooling Needs

* ✅ Deterministic deal path — shipped in Session 18c as a DEBUG-only `Deck(forcedDealOrder:)` + `GameState.setForcedDeck` + `DebugScenario` chain wired into `GameTableViewModel.deal()`. Hidden access: long-press the Main Menu title (1.5s) reveals `DebugMenuView` with three preset scenarios (`dealerDoesNotQualify`, `playerFlushOnRiver`, `push`). One-shot per arming — the scenario is consumed at the next DEAL and the deck reshuffles normally at hand end. Adding ceremony-tier scenarios (straight flush, royal flush) is deferred until phone-test feedback says they're needed; the three included scenarios specifically target the three Session 18a ⏳ items and nothing more (per the maintenance reminder in the Session 18c prompt).

### Debug menu — access and arming

**To reach the debug menu on device or simulator:** open the app, long-press the "6th Seat Hold'em" title on the Main Menu screen for ~1.5 seconds. A sheet titled "Debug Menu" slides up with three buttons under "Force the next deal":

* **Dealer fails to qualify** — Player K-high vs. dealer J-high on a board with no pairs; dealer hand-evaluates to `.highCard` and `DealerQualification.qualifies` returns false. Verifies the dealer-doesn't-qualify code path (Ante pushes, Play pays 1:1).
* **Player flush on river** — Player A♥ K♥ vs. dealer Q♠ Q♦; community 7♥ 2♠ 9♥ 3♣ J♥. The fifth heart arrives on the river — verifies that the hand-strength readout, headline, and ceremony tier all key off the river card.
* **Player flush on river** also exercises a winning hand with a qualifying dealer (pair of queens), so the chip-resolution choreography fires on a clear win.
* **Push (tie on a straight)** — Player 2♥ 3♥ vs. dealer 2♣ 3♣; community A♠ K♠ Q♦ J♥ 10♥ (broadway). Both players play the board, both have the same A-high straight, Ante and Play push, Blind pushes (UTH ties), Trips pays 5:1 if placed. Verifies the push headline and Trips-on-push payout.

Tap a scenario to arm it (an `ARMED` accent appears next to the row). Close the sheet, place your bets normally, tap DEAL — the next hand uses the forced sequence. The scenario auto-clears as soon as DEAL fires, so a second hand reshuffles to a normal random deck unless you re-arm. Force-quitting the app also clears the buffer (memory-only — see SPEC's Architectural Decisions for the storage rationale).

The debug menu and every supporting type are wrapped in `#if DEBUG`. Release builds (App Store / TestFlight production) carry zero references to them — verified in Session 18c by running `strings` on the Release `SixthSeatApp` binary and finding zero hits for `DebugDealForcer`, `DebugScenario`, `DebugMenuView`, `forcedDealOrder`, or `setForcedDeck`.

## Sustainability Check

Sessions 11, 14, 14a, 12 in close succession. Session 12 was reactive to phone-test feedback rather than a planned slot — it reversed the earlier "struck" decision because the casino-realism issue read as more important on hardware than on paper. Real next step is a phone test pass on the new deal sequence + the still-outstanding dealer view-identity work (14c), then Session 15.

## Session 14b notes

**Session 14b — Housekeeping (no commit).** Dropped stale `main-pbxproj-reorder-pre-session14-merge` stash. Audited dealer-card view identity pattern: **finding — dealer cards are missing both halves of the per-deal SwiftUI identity convention** (no `.id("dealer-card-...")` on the dealer `CardView`s in `GameTableView.swift`, and no `await Task.yield()` at the top of `animateDealerHoleCards` in `GameTableViewModel.swift`). Deferred to a focused follow-up session per the same reasoning that produced Session 14a — dealer face-down state interacts with fold path and Session 11's no-reveal rule, so it deserves regression tests, not a drive-by. Updated handoff doc. Confirmed simulator plist shows starter-bonus-applied state (`chipBalance = 0`, `starterBonus = 1`).
